const PROMPT="光る順番を覚えて",HELP="あとで同じ順番にタップ。",RECALL_HELP="同じ順番でタップしてください。";
const DURATION=7200,RECALL_MS=5000,FLASH_START_MS=600,FLASH_STEP_MS=1300,FLASH_ON_MS=750,CHOSEN_MS=180;
const CELLS=Object.freeze([0,1,2,3,4,5,6,7,8]);
const metadata=Object.freeze({id:"memory-path-v1",introducedIn:"1.0",tier:1,flavor:"classic",step:1,family:"memory-path",category:"memory"});
const clamp=(value,min=0,max=1)=>Math.max(min,Math.min(max,value));
const exactPermutation=value=>Array.isArray(value)&&value.length===9&&value.every(Number.isSafeInteger)&&new Set(value).size===9&&value.every(cell=>cell>=0&&cell<=8);
function generate(helpers={}){let shuffled;try{shuffled=helpers.shuffle([...CELLS])}catch{shuffled=null}if(!exactPermutation(shuffled))shuffled=[...CELLS];return{kind:"memoryPath",prompt:PROMPT,help:HELP,path:shuffled.slice(0,3),duration:DURATION}}
function validate(task){
 if(!task||typeof task!=="object"||Array.isArray(task))return["task must be a plain memoryPath object"];
 const issues=[],expected=task.recallAfterMs===undefined?["duration","help","kind","path","prompt"]:["duration","help","kind","path","prompt","recallAfterMs"],keys=Object.keys(task).sort();
 if(keys.length!==expected.length||keys.some((key,index)=>key!==expected[index]))issues.push("task fields must remain exactly kind, prompt, help, path, duration, with only optional recallAfterMs");
 if(task.kind!=="memoryPath")issues.push("kind must remain memoryPath");if(task.prompt!==PROMPT)issues.push("prompt changed");if(task.help!==HELP)issues.push("help changed");if(task.duration!==DURATION)issues.push("duration must remain 7200ms");
 if(!Array.isArray(task.path)||task.path.length!==3||task.path.some(cell=>!Number.isSafeInteger(cell)||cell<0||cell>8)||new Set(task.path).size!==3)issues.push("path must contain three distinct cell integers from 0 through 8");
 if(task.recallAfterMs!==undefined&&(!Number.isSafeInteger(task.recallAfterMs)||task.recallAfterMs<=0))issues.push("recallAfterMs must be a positive safe integer when supplied");return issues;
}
const CSS=`
.mpv-stage{box-sizing:border-box;width:100%;max-width:430px;margin-inline:auto;color:#392d42;outline:none;font-family:"Hiragino Kaku Gothic ProN","Yu Gothic",system-ui,sans-serif}.mpv-stage *{box-sizing:border-box}.mpv-instrument{position:relative;padding:1rem 1rem 1.05rem;border:1px solid #cdbbd5;border-radius:1.45rem;background:radial-gradient(circle at 50% 6%,rgba(255,255,255,.16),transparent 31%),linear-gradient(150deg,#5b3d62,#33283e 48%,#241d2c);box-shadow:inset 0 1px rgba(255,255,255,.28),inset 0 -1.1rem 2rem rgba(15,9,20,.2),0 .75rem 1.6rem rgba(62,42,72,.2);isolation:isolate}.mpv-instrument::before{content:"";position:absolute;inset:.4rem;border:1px solid rgba(226,205,232,.2);border-radius:1.12rem;pointer-events:none}.mpv-screw{position:absolute;z-index:2;width:.5rem;height:.5rem;border-radius:50%;background:radial-gradient(circle at 35% 30%,#f1dca4,#9b7543 42%,#4d3428);box-shadow:inset 0 0 0 1px rgba(36,21,20,.45)}.mpv-screw.a{left:.65rem;top:.65rem}.mpv-screw.b{right:.65rem;top:.65rem}.mpv-screw.c{left:.65rem;bottom:.65rem}.mpv-screw.d{right:.65rem;bottom:.65rem}.mpv-grid{position:relative;z-index:1;display:grid;grid-template-columns:repeat(3,1fr);gap:.64rem;width:100%;max-width:18rem;margin:0 auto;padding:.48rem;border:1px solid rgba(225,203,231,.25);border-radius:1.22rem;background:linear-gradient(145deg,rgba(20,13,25,.3),rgba(102,76,109,.18));box-shadow:inset 0 .28rem .75rem rgba(14,8,18,.34);outline:none}.mpv-tile{position:relative;display:grid;place-items:center;min-width:56px;min-height:56px;aspect-ratio:1;padding:0;border:2px solid #d7c5e1;border-radius:1.08rem;background:radial-gradient(circle at 34% 26%,#fff 0 6%,#f4eef7 8% 46%,#ded1e4 72%,#bca9c5 100%);box-shadow:inset .18rem .18rem .32rem rgba(255,255,255,.8),inset -.22rem -.25rem .35rem rgba(73,50,82,.14),0 .34rem .55rem rgba(16,9,20,.25);touch-action:manipulation;cursor:pointer}.mpv-tile::before{content:"";position:absolute;inset:.36rem;border:1px solid rgba(106,76,119,.16);border-radius:.76rem}.mpv-tile::after{content:"";width:1.45rem;height:1.45rem;opacity:0;background:#fff;clip-path:polygon(50% 0,61% 38%,100% 50%,61% 62%,50% 100%,39% 62%,0 50%,39% 38%);filter:drop-shadow(0 0 .45rem #fff)}.mpv-tile:disabled{opacity:1;cursor:default}.mpv-tile.flash{z-index:2;border:3px solid #6f3e86;background:radial-gradient(circle at 38% 31%,#fff 0 9%,#ead1f3 11%,#b574cf 51%,#704184 100%);box-shadow:0 0 0 4px rgba(255,255,255,.95),0 0 0 7px rgba(128,81,154,.48),0 0 1.8rem rgba(176,104,207,.72);transform:scale(1.08)}.mpv-tile.flash::after{opacity:1}.mpv-tile.chosen{border-color:#85549b;background:linear-gradient(145deg,#dcb9eb,#a76bc2);box-shadow:inset 0 .2rem .35rem rgba(60,30,72,.26),0 .12rem .3rem rgba(30,17,38,.22);transform:translateY(2px) scale(.96)}.mpv-tile.wrong{border-color:#a5435b;background:linear-gradient(145deg,#f5cbd4,#de879b);box-shadow:inset 0 0 0 3px rgba(128,35,57,.16),0 0 1rem rgba(187,64,91,.36)}.mpv-tile.wrong::after{opacity:1;width:2rem;height:2rem;background:#8e3049;clip-path:polygon(46% 0,56% 37%,87% 16%,64% 48%,100% 54%,64% 61%,82% 100%,54% 68%,39% 100%,43% 62%,0 52%,40% 48%,18% 18%,45% 38%);filter:none}.mpv-tile:focus-visible{outline:3px dashed #f2c768;outline-offset:4px}.mpv-plaque{position:relative;z-index:1;min-height:3.15rem;margin:.72rem .12rem 0;padding:.58rem .72rem;border:1px solid rgba(238,216,177,.44);border-radius:.82rem;background:linear-gradient(145deg,rgba(27,19,32,.92),rgba(62,43,67,.9));color:#fff1cc;text-align:center;font-size:.78rem;font-weight:900;line-height:1.42;box-shadow:inset 0 1px rgba(255,255,255,.1)}.mpv-plaque::before{content:"MEMORY LAMP";display:block;margin-bottom:.12rem;color:#c8a767;font-size:.52rem;letter-spacing:.18em}.mpv-stage.wrong .mpv-plaque{border-color:#d98293;color:#ffd9dd}.mpv-stage.success .mpv-plaque{border-color:#79b89d;color:#d8f6e8}.mpv-stage.timeout .mpv-instrument{filter:saturate(.62)}.mpv-stage.done .mpv-tile{cursor:default}.mpv-stage[data-reduced=true] *{animation:none!important;transition:none!important}@media(prefers-reduced-motion:reduce){.mpv-stage *{animation:none!important;transition:none!important}}
`;
function render(task,context){
 const issues=validate(task);if(issues.length)throw new Error(`${metadata.id}: ${issues.join("; ")}`);const doc=context.host?.ownerDocument;if(!doc?.createElement)throw new TypeError(`${metadata.id}: context.host.ownerDocument is required`);const make=(tag,className,text)=>{const node=doc.createElement(tag);if(className)node.className=className;if(text!==undefined)node.textContent=text;return node},style=make("style"),stage=make("section","mpv-stage observe"),instrument=make("div","mpv-instrument"),grid=make("div","mpv-grid"),plaque=make("p","mpv-plaque",HELP);style.textContent=CSS;stage.tabIndex=0;stage.dataset.reduced=String(Boolean(context.reducedMotion));stage.setAttribute("role","application");stage.setAttribute("aria-label","3行3列の光る順番記憶");grid.setAttribute("role","grid");grid.setAttribute("aria-label","光る順番を覚える9マス");plaque.setAttribute("role","status");plaque.setAttribute("aria-live","polite");for(const className of["a","b","c","d"])instrument.append(make("i",`mpv-screw ${className}`));const buttons=[];for(let cell=0;cell<9;cell++){const button=make("button","mpv-tile");button.type="button";button.disabled=true;button.dataset.cell=String(cell);button.setAttribute("role","gridcell");button.setAttribute("aria-label",`${Math.floor(cell/3)+1}行${cell%3+1}列のマス`);buttons.push(button);grid.append(button)}instrument.append(grid,plaque);stage.append(instrument);context.host.replaceChildren(style,stage);
 const readNow=()=>{const value=typeof context.now==="function"?Number(context.now()):Number(globalThis.performance?.now?.());return Number.isFinite(value)?value:0},startedAt=readNow(),events=[],pulses=new Set(),state={phase:"observe",activeFlash:null,cursor:0,entered:[],focusCell:0,wrongCell:null,done:false,disposed:false,result:null,finishCalls:0};
 const event=(type,cell=null,step=null)=>events.push({type,cell,step,at:Math.max(0,readNow()-startedAt)});const refresh=()=>{stage.className=`mpv-stage ${state.phase}${state.done?" done":""}`;buttons.forEach((button,cell)=>{button.disabled=state.phase!=="recall"||state.done;button.classList.toggle("flash",state.activeFlash===cell);button.classList.toggle("chosen",pulses.has(cell));button.classList.toggle("wrong",state.wrongCell===cell)})};
 const finish=(correct,payload,phase)=>{if(state.done||state.disposed)return false;state.done=true;state.phase=phase;state.result={correct,payload:{...payload}};state.finishCalls++;plaque.textContent=payload.detail;event(phase);refresh();context.finish(correct,payload);return true};
 const choose=cell=>{if(state.done||state.disposed||state.phase!=="recall"||!Number.isSafeInteger(cell)||cell<0||cell>8)return false;const expected=task.path[state.cursor];if(cell!==expected){state.wrongCell=cell;event("wrong",cell,state.cursor);return finish(false,{detail:"順番が迷子になりました。"},"wrong")}pulses.add(cell);state.entered.push(cell);event("correct",cell,state.cursor);state.cursor++;refresh();context.later(()=>{if(state.disposed)return;pulses.delete(cell);refresh()},CHOSEN_MS);if(state.cursor===task.path.length){const elapsed=readNow()-startedAt;return finish(true,{quality:clamp(1-elapsed/9000,0,1),detail:"順番どおりです。"},"success")}return true};
 task.path.forEach((cell,step)=>{const startsAt=FLASH_START_MS+step*FLASH_STEP_MS;context.later(()=>{if(state.done||state.disposed)return;state.activeFlash=cell;event("flash-on",cell,step);refresh()},startsAt);context.later(()=>{if(state.done||state.disposed)return;if(state.activeFlash===cell)state.activeFlash=null;event("flash-off",cell,step);refresh()},startsAt+FLASH_ON_MS)});
 const recallAt=task.recallAfterMs||RECALL_MS;context.later(()=>{if(state.done||state.disposed)return;state.phase="recall";state.activeFlash=null;plaque.textContent=RECALL_HELP;if(context.helpNode&&"textContent"in context.helpNode)context.helpNode.textContent=RECALL_HELP;event("recall");refresh();context.setDeadline(task.duration,()=>finish(false,{detail:"記憶が時間切れになりました。"},"timeout"))},recallAt);
 buttons.forEach((button,cell)=>{context.listen(button,"pointerdown",eventObject=>{eventObject.preventDefault();choose(cell)});context.listen(button,"click",eventObject=>{if(eventObject.detail===0){eventObject.preventDefault();choose(cell)}});context.listen(button,"focus",()=>{state.focusCell=cell})});context.listen(grid,"keydown",eventObject=>{if(state.phase!=="recall"||state.done)return;const active=buttons.indexOf(doc.activeElement),from=active>=0?active:state.focusCell;let to=from;if(eventObject.key==="ArrowLeft"&&from%3>0)to--;else if(eventObject.key==="ArrowRight"&&from%3<2)to++;else if(eventObject.key==="ArrowUp"&&from>=3)to-=3;else if(eventObject.key==="ArrowDown"&&from<6)to+=3;else return;eventObject.preventDefault();state.focusCell=to;buttons[to].focus({preventScroll:true})});context.listen(context.signal,"abort",()=>{state.disposed=true;state.done=true;state.activeFlash=null;pulses.clear();if(context.qa?.[metadata.id]===qaApi)delete context.qa[metadata.id]},{once:true});stage.focus({preventScroll:true});refresh();
 const qaApi={choose,focus:cell=>{if(state.phase!=="recall"||!Number.isSafeInteger(cell)||cell<0||cell>8)return false;state.focusCell=cell;buttons[cell].focus({preventScroll:true});return true},inspect:()=>({phase:state.phase,reduced:stage.dataset.reduced==="true",activeFlash:state.activeFlash,cursor:state.cursor,entered:[...state.entered],focusCell:state.focusCell,wrongCell:state.wrongCell,done:state.done,disposed:state.disposed,result:state.result?structuredClone(state.result):null,finishCalls:state.finishCalls,help:plaque.textContent,disabled:buttons.filter(button=>button.disabled).length,classes:buttons.map(button=>button.className),focused:buttons.indexOf(doc.activeElement),events:events.map(item=>({...item})),taskJSON:JSON.stringify(task)}),elements:{stage,grid,buttons,plaque}};if(context.qa&&typeof context.qa==="object")context.qa[metadata.id]=qaApi;
}
export default Object.freeze({metadata,generate,validate,render});

const DURATION=8000,TARGETS=Object.freeze(["🤓","😀","🐶","🤓","🤑","🐱","🐼","🤓"]),POSITIONS=Object.freeze([[21,25,-30,.9],[49,22,20,1.05],[76,28,-10,.95],[29,50,35,1.13],[67,52,5,1],[20,69,-20,.88],[49,72,45,1.16],[78,67,-5,.94]]);
const metadata=Object.freeze({id:"spatial-emoji-fps-v1",introducedIn:"1.0",tier:3,flavor:"wild",step:1,family:"spatial-emoji-fps",category:"spatial"});
const clamp=(value,min,max)=>Math.max(min,Math.min(max,value));
const exactMultiset=values=>Array.isArray(values)&&values.length===TARGETS.length&&[...values].sort().join("")===[...TARGETS].sort().join("");
function generate({shuffle}){if(typeof shuffle!=="function")throw new TypeError(`${metadata.id}: shuffle is required`);const candidate=shuffle([...TARGETS]);return{kind:"emojiFps",prompt:"🤓だけを3体ロックオン",help:"😀・🤑・動物は押さないで。",targets:exactMultiset(candidate)?[...candidate]:[...TARGETS],duration:DURATION}}
function validate(task){const issues=[];if(!task||typeof task!=="object")return["task must be an object"];const keys=Object.keys(task).sort(),expected=["duration","help","kind","prompt","targets"].sort();if(keys.length!==expected.length||keys.some((key,index)=>key!==expected[index]))issues.push("task fields changed");if(task.kind!=="emojiFps")issues.push("kind changed");if(task.prompt!=="🤓だけを3体ロックオン")issues.push("prompt changed");if(task.help!=="😀・🤑・動物は押さないで。")issues.push("help changed");if(task.duration!==DURATION)issues.push("duration must remain 8000ms");if(!exactMultiset(task.targets))issues.push("targets must preserve the published shuffled multiset");return[...new Set(issues)]}

const STYLE=`
.sef-stage{box-sizing:border-box;width:100%;max-width:430px;margin:auto;display:grid;gap:.48rem;color:#3d3341;contain:layout}.sef-status{min-height:2.6em;margin:0;padding:.46rem .68rem;border:1px solid #d5c3df;border-radius:.86rem;background:linear-gradient(#fbf6fd,#eee3f3);color:#684e72;text-align:center;font-size:clamp(.82rem,3.45vw,.96rem);font-weight:950;line-height:1.35}.sef-status[data-tone=good]{background:#e4f3e8;border-color:#9fc8aa;color:#326344}.sef-status[data-tone=bad]{background:#f7e3e7;border-color:#d9a0aa;color:#893846}.sef-arena{--stage-perspective:430px;position:relative;width:100%;height:14rem;overflow:hidden;border:1.5px solid #674875;border-radius:1.25rem;background:radial-gradient(ellipse at 50% 48%,#725182 0 8%,#3c2948 30%,#1b1122 76%);perspective:var(--stage-perspective);isolation:isolate;box-shadow:inset 0 0 38px rgba(0,0,0,.48),0 10px 24px rgba(48,29,57,.24);outline:none}.sef-arena:focus-visible{box-shadow:0 0 0 3px #fff5d8,0 0 0 6px #8a58a0,inset 0 0 38px rgba(0,0,0,.48),0 10px 24px rgba(48,29,57,.24)}.sef-world{position:absolute;inset:0;transform-style:preserve-3d;pointer-events:none}.sef-arena::before,.sef-arena::after{content:"";position:absolute;z-index:0;left:50%;width:145%;height:78%;border:2px solid rgba(220,183,235,.32);transform-origin:center}.sef-arena::before{top:-38%;transform:translateX(-50%) rotateX(69deg);background:repeating-linear-gradient(90deg,transparent 0 11%,rgba(211,170,229,.22) 12%),repeating-linear-gradient(0deg,transparent 0 18%,rgba(211,170,229,.2) 19%)}.sef-arena::after{bottom:-38%;transform:translateX(-50%) rotateX(-69deg);background:repeating-linear-gradient(90deg,transparent 0 11%,rgba(211,170,229,.24) 12%),repeating-linear-gradient(0deg,transparent 0 18%,rgba(211,170,229,.22) 19%)}.sef-tunnel{position:absolute;inset:0;z-index:1;background:repeating-conic-gradient(from 2deg at 50% 48%,transparent 0 13deg,rgba(224,195,238,.21) 14deg 15deg,transparent 16deg 44deg)}.sef-tunnel::before{content:"";position:absolute;left:50%;top:48%;width:21%;height:24%;transform:translate(-50%,-50%);border:2px solid rgba(242,222,250,.52);box-shadow:0 0 0 24px rgba(219,181,234,.055),0 0 0 49px rgba(219,181,234,.045),0 0 0 75px rgba(219,181,234,.035),0 0 28px rgba(222,188,238,.24)}.sef-tunnel::after{content:"";position:absolute;inset:8%;border:1px solid rgba(255,241,255,.1);border-radius:42%;box-shadow:inset 0 0 22px rgba(241,215,250,.08)}.sef-crosshair{position:absolute;z-index:5;left:50%;top:48%;width:3.2rem;height:3.2rem;transform:translate(-50%,-50%);border:1px solid rgba(255,255,255,.26);border-radius:50%;color:rgba(255,255,255,.76);display:grid;place-items:center;font-size:2.65rem;font-weight:200;line-height:1;text-shadow:0 0 8px #ddb8ee;pointer-events:none}.sef-crosshair::before,.sef-crosshair::after{content:"";position:absolute;background:rgba(255,245,255,.62);box-shadow:0 0 6px #e1bced}.sef-crosshair::before{width:4.2rem;height:1px}.sef-crosshair::after{width:1px;height:4.2rem}.sef-entity{position:absolute;left:var(--entity-x);top:var(--entity-y);z-index:6;width:3.15rem;height:3.15rem;padding:0;border:1.5px solid rgba(235,215,244,.68);border-radius:50%;background:radial-gradient(circle at 35% 28%,rgba(255,255,255,.19),rgba(53,31,63,.68) 56%,rgba(21,12,26,.86));color:inherit;font:1.85rem/1 "Apple Color Emoji","Segoe UI Emoji","Noto Color Emoji",sans-serif;display:grid;place-items:center;transform:translate(-50%,-50%) translateZ(var(--entity-z)) scale(var(--entity-scale));transform-style:preserve-3d;filter:drop-shadow(0 5px 4px rgba(18,8,23,.42));box-shadow:inset 0 0 0 2px rgba(255,255,255,.06),0 0 0 3px rgba(185,120,215,.08);pointer-events:auto;touch-action:manipulation;cursor:crosshair;animation:sef-float 1.2s ease-in-out var(--float-delay) infinite alternate}.sef-entity::after{content:attr(data-depth);position:absolute;right:-.18rem;bottom:-.18rem;padding:.08rem .2rem;border:1px solid rgba(238,215,247,.3);border-radius:999px;background:rgba(22,12,28,.8);color:#ead6f2;font:800 .48rem/1 ui-monospace,monospace;letter-spacing:.02em}.sef-entity[data-spatial=true]{border-color:#ffe58a;box-shadow:0 0 0 3px rgba(255,229,138,.32),0 0 15px rgba(236,199,100,.72),inset 0 0 0 2px rgba(255,255,255,.12)}.sef-entity:focus-visible{outline:3px solid #fff1ae;outline-offset:3px}.sef-entity[data-locking=true]{border-color:#bff1d0;box-shadow:0 0 0 calc(2px + var(--lock-progress)*7px) rgba(148,240,186,calc(.18 + var(--lock-progress)*.36)),0 0 calc(8px + var(--lock-progress)*22px) #9debbb;opacity:calc(1 - var(--lock-progress)*.34)}.sef-entity[data-bystander=true]{border-color:#ff9b9c;box-shadow:0 0 0 5px rgba(224,75,84,.35),0 0 22px #e3535d}.sef-entity:disabled{cursor:default}.sef-counter{position:absolute;z-index:8;right:.55rem;bottom:.45rem;padding:.28rem .54rem;border:1px solid rgba(235,213,244,.4);border-radius:999px;background:rgba(24,14,29,.8);color:#f0ddf8;font:850 .64rem/1 ui-monospace,SFMono-Regular,monospace;letter-spacing:.09em;box-shadow:0 4px 12px rgba(0,0,0,.24)}.sef-lock-note{position:absolute;z-index:8;left:.55rem;bottom:.45rem;padding:.24rem .48rem;border-radius:999px;background:rgba(24,14,29,.7);color:#dbc4e5;font-size:.58rem;font-weight:850;letter-spacing:.05em}.sef-terminal{position:absolute;z-index:10;left:50%;top:.55rem;transform:translateX(-50%);min-width:64%;padding:.44rem .72rem;border:1px solid rgba(255,255,255,.44);border-radius:.8rem;background:rgba(117,42,55,.94);color:#fff4ed;text-align:center;font-size:.88rem;font-weight:1000;box-shadow:0 6px 18px rgba(18,8,20,.34)}.sef-arena[data-result=success] .sef-terminal{background:rgba(44,108,68,.95);color:#efffe9}.sef-arena[data-result=timeout] .sef-terminal{background:rgba(67,54,91,.95)}.sef-hint{margin:0;text-align:center;color:#746678;font-size:.7rem;font-weight:800;line-height:1.35}@keyframes sef-float{to{margin-top:-6px;filter:drop-shadow(0 8px 5px rgba(0,0,0,.32)) brightness(1.1)}}.sef-stage[data-reduced=true] .sef-entity{animation:none!important;transition:none!important}
`;

function render(task,context){
  const issues=validate(task);if(issues.length)throw new Error(`${metadata.id}: ${issues.join("; ")}`);const documentRef=context.host?.ownerDocument;if(!documentRef?.createElement)throw new TypeError(`${metadata.id}: context.host.ownerDocument is required`);const view=documentRef.defaultView,now=()=>Number(view?.performance?.now?.()??globalThis.performance?.now?.()??0);
  const style=documentRef.createElement("style");style.textContent=STYLE;const stage=documentRef.createElement("section");stage.className="sef-stage";stage.dataset.reduced=String(Boolean(context.reducedMotion));const status=documentRef.createElement("p");status.className="sef-status";status.dataset.tone="plain";status.setAttribute("role","status");status.setAttribute("aria-live","polite");const arena=documentRef.createElement("div");arena.className="sef-arena";arena.tabIndex=0;arena.setAttribute("role","application");arena.setAttribute("aria-label","絵文字をロックオンする3D通路");arena.dataset.result="";const world=documentRef.createElement("div");world.className="sef-world";const tunnel=documentRef.createElement("div");tunnel.className="sef-tunnel";const crosshair=documentRef.createElement("span");crosshair.className="sef-crosshair";crosshair.textContent="＋";crosshair.setAttribute("aria-hidden","true");const counter=documentRef.createElement("span");counter.className="sef-counter";counter.setAttribute("aria-live","polite");const note=documentRef.createElement("span");note.className="sef-lock-note";note.textContent="NERD IDENTIFY / SAFE LOCK";const terminal=documentRef.createElement("strong");terminal.className="sef-terminal";terminal.hidden=true;world.append(tunnel,crosshair,counter,note,terminal);arena.append(world);const hint=documentRef.createElement("p");hint.className="sef-hint";hint.textContent="タップ　｜　矢印キーで空間選択　Enterでロック";stage.append(status,arena,hint);context.host.replaceChildren(style,stage);
  const entities=task.targets.map((emoji,index)=>{const button=documentRef.createElement("button"),[x,y,z,scale]=POSITIONS[index];button.type="button";button.className="sef-entity";button.id=`sef-target-${index}`;button.textContent=emoji;button.dataset.index=String(index);button.dataset.depth=`Z ${z>=0?"+":""}${z}`;button.dataset.spatial="false";button.dataset.locking="false";button.dataset.bystander="false";button.style.setProperty("--entity-x",`${x}%`);button.style.setProperty("--entity-y",`${y}%`);button.style.setProperty("--entity-z",`${z}px`);button.style.setProperty("--entity-scale",String(scale));button.style.setProperty("--float-delay",`${index*-.18}s`);button.style.setProperty("--lock-progress","0");button.setAttribute("aria-label",`${index+1}番 ${emoji}をロックオン、位置${x}・${y}、深度${z}`);world.append(button);return button});
  const state={active:Array(8).fill(true),hits:0,busy:false,locking:null,done:false,disposed:false,result:null,failedIndex:null,focusIndex:0,keyboard:false,frames:0};let token=0,frameStop=null,completeFn=null;const startedAt=now();
  const quality=()=>clamp(1-(now()-startedAt)/task.duration,0,1);
  const update=()=>{counter.textContent=`LOCK ${state.hits} / 3`;stage.dataset.busy=String(state.busy);arena.dataset.result=state.result||"";entities.forEach((button,index)=>{button.hidden=!state.active[index];button.disabled=state.busy||state.done||!state.active[index];button.dataset.spatial=String(state.keyboard&&index===state.focusIndex&&state.active[index]);button.dataset.locking=String(state.locking?.index===index);button.dataset.bystander=String(state.failedIndex===index);button.style.setProperty("--lock-progress",String(state.locking?.index===index?state.locking.progress:0))});arena.setAttribute("aria-activedescendant",state.keyboard&&state.active[state.focusIndex]?entities[state.focusIndex].id:"")};
  const terminalResult=(result,index=null)=>{if(state.done||state.disposed)return false;token++;state.done=true;state.busy=false;state.locking=null;completeFn=null;state.result=result;state.failedIndex=index;terminal.hidden=false;status.dataset.tone=result==="success"?"good":"bad";if(result==="success"){status.textContent="LOCK 3 / 3　対象だけを確保しました";terminal.textContent="TARGET CLEAR・🤓 3 / 3"}else if(result==="timeout"){status.textContent="時間切れ。未確保の対象が通路へ消えました";terminal.textContent="TIME OUT・通路閉鎖"}else{const emoji=task.targets[index];status.textContent=`${emoji}は一般通行人でした。`;terminal.textContent=`BYSTANDER・${emoji}は対象外`}update();const payload=result==="success"?{quality:quality(),detail:"🤓を全員ロックオンしました。"}:result==="timeout"?{detail:"🤓が3D通路へ消えました。"}:{detail:`${task.targets[index]}は一般通行人でした。`};context.finish(result==="success",payload);return true};
  const settleLock=record=>{if(state.disposed||state.done||state.locking!==record)return false;state.active[record.index]=false;state.hits++;state.busy=false;state.locking=null;completeFn=null;if(state.hits===3)return terminalResult("success");status.dataset.tone="plain";status.textContent=`LOCK ${state.hits} / 3　次の🤓を探してください`;if(!state.active[state.focusIndex]){const next=state.active.findIndex(Boolean);state.focusIndex=next<0?0:next}update();return true};
  const startLock=index=>{if(state.done||state.disposed||state.busy||!state.active[index])return false;if(task.targets[index]!=="🤓")return terminalResult("bystander",index);const record={index,progress:0,started:null,token:++token};state.busy=true;state.locking=record;state.focusIndex=index;status.dataset.tone="plain";status.textContent=`${index+1}番を照合中…`;completeFn=()=>{if(state.locking!==record)return false;record.progress=1;update();return settleLock(record)};update();if(context.reducedMotion){context.later(()=>{if(state.locking===record){record.progress=.45;update()}},70);context.later(()=>{if(state.locking===record){record.progress=.8;update()}},140);context.later(()=>{if(state.locking===record)completeFn?.()},220)}else if(!frameStop)frameStop=context.frame(time=>{if(state.disposed){frameStop=null;return false}state.frames++;const current=state.locking;if(!current){frameStop=null;return false}if(current.started===null)current.started=time;current.progress=Math.min(1,(time-current.started)/260);update();if(current.progress>=1)completeFn?.();const keep=Boolean(state.locking&&!state.disposed&&!state.done);if(!keep)frameStop=null;return keep});return true};
  const completeTransition=()=>completeFn?.()||false;
  const focusTarget=(index,keyboard=true)=>{if(state.done||state.busy||!Number.isInteger(index)||index<0||index>=entities.length||!state.active[index])return false;state.focusIndex=index;state.keyboard=keyboard;update();if(keyboard)arena.focus({preventScroll:true});return true};
  const spatialMove=direction=>{if(state.done||state.busy)return false;const active=state.active.map((value,index)=>value?index:-1).filter(index=>index>=0);if(!active.length)return false;let current=state.active[state.focusIndex]?state.focusIndex:active[0];const [cx,cy]=POSITIONS[current],vectors={left:[-1,0],right:[1,0],up:[0,-1],down:[0,1]},[vx,vy]=vectors[direction]||[0,0],candidates=active.filter(index=>index!==current).map(index=>{const [x,y]=POSITIONS[index],dx=x-cx,dy=y-cy,primary=dx*vx+dy*vy,orthogonal=Math.abs(dx*vy-dy*vx);return{index,primary,score:primary+orthogonal*.72}}).filter(value=>value.primary>0).sort((a,b)=>a.score-b.score||a.primary-b.primary);current=candidates[0]?.index??current;return focusTarget(current,true)};
  const bindEntity=(button,index)=>{context.listen(button,"pointerdown",event=>{event.preventDefault();event.stopPropagation();state.keyboard=false;state.focusIndex=index;startLock(index)});context.listen(button,"click",event=>{if(event.detail===0){event.preventDefault();event.stopPropagation();state.keyboard=true;state.focusIndex=index;startLock(index)}});context.listen(button,"focus",()=>{if(!state.done&&!state.busy){state.keyboard=true;state.focusIndex=index;update()}})};entities.forEach(bindEntity);
  context.listen(arena,"keydown",event=>{if(state.done||state.busy)return;const key=event.key;if(["ArrowLeft","ArrowRight","ArrowUp","ArrowDown"].includes(key)){event.preventDefault();spatialMove(key.slice(5).toLowerCase())}else if(key==="Enter"||key===" "){event.preventDefault();state.keyboard=true;startLock(state.focusIndex)}});
  status.textContent="3D通路の🤓だけを選びます";update();arena.focus({preventScroll:true});const expire=()=>terminalResult("timeout");context.setDeadline(task.duration,expire);
  const inspect=()=>({targets:task.targets.map((emoji,index)=>({index,emoji,position:[...POSITIONS[index]],active:state.active[index]})),active:[...state.active],removed:state.active.map((active,index)=>active?-1:index).filter(index=>index>=0),hits:state.hits,busy:state.busy,locking:state.locking?{index:state.locking.index,progress:state.locking.progress}:null,done:state.done,disposed:state.disposed,result:state.result,failedIndex:state.failedIndex,focusIndex:state.focusIndex,keyboard:state.keyboard,frames:state.frames,status:status.textContent,counter:counter.textContent,viewport:{...context.viewport},dpr:Math.max(1,Math.min(3,Number(context.viewport?.dpr)||Number(view?.devicePixelRatio)||1)),hitSizeRem:3.15});
  const qaApi={select:startLock,focus:focusTarget,move:spatialMove,completeTransition,expire,inspect};if(context.qa&&typeof context.qa==="object")context.qa[metadata.id]=qaApi;context.listen(context.signal,"abort",()=>{state.disposed=true;state.busy=false;state.locking=null;completeFn=null;token++;frameStop?.();if(context.qa?.[metadata.id]===qaApi)delete context.qa[metadata.id]},{once:true});
}
export default Object.freeze({metadata,generate,validate,render});

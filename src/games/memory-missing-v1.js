const POOL=Object.freeze(["🍇","鍵","傘","月","猫","靴","山","時計","魚","本"]),PROMPT="なかったものを選んで",HELP="まず5つを覚えてください。",RECALL_HELP="さっき、なかったものは？",EXPOSURE_MS=5000,DURATION=7500,FEEDBACK_MS=180,TIMEOUT_DETAIL="時間切れです。脳は定時退社しました。";
const metadata=Object.freeze({id:"memory-missing-v1",introducedIn:"1.0",tier:1,flavor:"quirky",step:1,family:"memory-missing",category:"memory"});
const permutationOf=(values,pool)=>Array.isArray(values)&&values.length===pool.length&&new Set(values).size===pool.length&&values.every(value=>pool.includes(value)),validShown=shown=>Array.isArray(shown)&&shown.length===5&&new Set(shown).size===5&&shown.every(value=>POOL.includes(value)),validOptions=(options,shown,answer)=>Array.isArray(options)&&options.length===4&&new Set(options).size===4&&options.includes(answer)&&options.every(value=>POOL.includes(value))&&options.filter(value=>value!==answer).every(value=>shown.includes(value));
function generate(helpers={}){let shuffledAll;try{shuffledAll=helpers.shuffle([...POOL])}catch{shuffledAll=null}if(!permutationOf(shuffledAll,POOL))shuffledAll=[...POOL];const shown=shuffledAll.slice(0,5),absentPool=POOL.filter(value=>!shown.includes(value));let answer;try{answer=helpers.pick([...absentPool])}catch{answer=null}if(!absentPool.includes(answer))answer=absentPool[0];let shuffledShown;try{shuffledShown=helpers.shuffle([...shown])}catch{shuffledShown=null}if(!permutationOf(shuffledShown,shown))shuffledShown=[...shown];const candidates=[answer,...shuffledShown.slice(0,3)];let options;try{options=helpers.shuffle([...candidates])}catch{options=null}if(!permutationOf(options,candidates))options=[...candidates];return{kind:"flashChoice",prompt:PROMPT,help:HELP,shown:[...shown],options:[...options],answer,duration:DURATION}}
function validate(task){
 if(!task||typeof task!=="object"||Array.isArray(task))return["task must be a plain flashChoice object"];const issues=[],expected=["answer","duration","help","kind","options","prompt","shown"],keys=Object.keys(task).sort();
 if(keys.length!==expected.length||keys.some((key,index)=>key!==expected[index]))issues.push("task fields must remain exactly kind, prompt, help, shown, options, answer, duration");if(task.kind!=="flashChoice")issues.push("kind must remain flashChoice");if(task.prompt!==PROMPT)issues.push("prompt changed");if(task.help!==HELP)issues.push("help changed");if(task.duration!==DURATION)issues.push("duration must remain 7500ms");if(!validShown(task.shown))issues.push("shown must remain five unique authored pool values");if(!POOL.includes(task.answer)||task.shown?.includes?.(task.answer))issues.push("answer must be one authored value absent from shown");if(!validShown(task.shown)||!validOptions(task.options,task.shown,task.answer))issues.push("options must contain the absent answer and exactly three shown distractors");return issues;
}
const CSS=`
.mmv-stage{box-sizing:border-box;width:100%;max-width:430px;margin-inline:auto;color:#392d42;outline:none;font-family:"Hiragino Kaku Gothic ProN","Yu Gothic",system-ui,sans-serif}.mmv-stage *{box-sizing:border-box}.mmv-console{position:relative;padding:.94rem .84rem .88rem;border:1px solid #c2b2cb;border-radius:1.4rem;background:radial-gradient(circle at 50% -10%,rgba(255,255,255,.23),transparent 38%),linear-gradient(150deg,#5c4665,#382c42 55%,#241d2b);box-shadow:inset 0 1px rgba(255,255,255,.27),inset 0 -1rem 2rem rgba(13,8,17,.24),0 .72rem 1.55rem rgba(57,39,66,.2);isolation:isolate}.mmv-console::before{content:"";position:absolute;inset:.4rem;border:1px solid rgba(235,222,240,.18);border-radius:1.08rem;pointer-events:none}.mmv-label{position:relative;z-index:1;margin:0 0 .5rem;color:#e3c780;text-align:center;font-size:.57rem;font-weight:950;letter-spacing:.18em}.mmv-tray{position:relative;z-index:1;padding:.7rem .58rem;border:1px solid rgba(235,221,240,.34);border-radius:1.05rem;background:linear-gradient(145deg,rgba(16,10,20,.5),rgba(103,76,112,.2));box-shadow:inset 0 .28rem .8rem rgba(12,7,15,.38)}.mmv-row{display:grid;grid-template-columns:repeat(5,minmax(0,1fr));gap:.38rem}.mmv-item{position:relative;display:grid;place-items:center;min-width:0;min-height:4.2rem;padding:.18rem;border:1px solid rgba(244,238,245,.72);border-radius:.78rem;background:radial-gradient(circle at 34% 22%,#fff 0 8%,#f6f1f6 10% 50%,#ddd3e1 74%,#a99bad);color:#3c2946;font:950 clamp(.88rem,5.1vw,1.32rem)/1.05 "Apple Color Emoji","Segoe UI Emoji","Noto Color Emoji","Yu Gothic",system-ui,sans-serif;text-shadow:0 1px #fff;box-shadow:inset .13rem .13rem .25rem rgba(255,255,255,.88),inset -.15rem -.18rem .28rem rgba(52,37,59,.14),0 .26rem .48rem rgba(8,5,11,.28)}.mmv-item.covered{background:linear-gradient(145deg,#d8cedc,#afa1b5);box-shadow:inset .14rem .14rem .28rem rgba(255,255,255,.45),inset -.18rem -.2rem .32rem rgba(43,31,49,.2),0 .24rem .45rem rgba(8,5,11,.25)}.mmv-item.covered::after{content:"";position:absolute;inset:.36rem;border:1px solid rgba(78,59,86,.2);border-radius:.48rem;background:repeating-linear-gradient(135deg,rgba(255,255,255,.08) 0 4px,rgba(70,51,79,.05) 4px 8px)}.mmv-phase{position:relative;z-index:1;min-height:1.45rem;margin:.58rem .2rem .5rem;color:#eee3f2;text-align:center;font-size:.73rem;font-weight:900;line-height:1.35}.mmv-choices{position:relative;z-index:1;display:grid;grid-template-columns:1fr 1fr;gap:.58rem}.mmv-choices[hidden]{display:none!important}.mmv-choice{position:relative;display:grid;place-items:center;min-height:4.75rem;padding:.42rem;border:1.5px solid #d8cddd;border-radius:1rem;background:radial-gradient(circle at 35% 20%,#fff 0 8%,#f8f4f9 10% 53%,#e4dce7 77%,#c2b5c7);color:#4a3355;font:950 clamp(1.35rem,7vw,1.85rem)/1.1 "Apple Color Emoji","Segoe UI Emoji","Noto Color Emoji","Yu Gothic",system-ui,sans-serif;box-shadow:inset .15rem .15rem .3rem rgba(255,255,255,.88),inset -.18rem -.2rem .32rem rgba(57,40,64,.13),0 .34rem .64rem rgba(10,6,13,.27);touch-action:manipulation;cursor:pointer}.mmv-choice::before{content:"";position:absolute;inset:.3rem;border:1px solid rgba(78,57,87,.11);border-radius:.72rem}.mmv-choice:hover:not(:disabled){border-color:#b39cbd}.mmv-choice:focus-visible{outline:3px dashed #f2ca67;outline-offset:3px}.mmv-choice:active:not(:disabled),.mmv-choice.selected{transform:translateY(2px) scale(.985);box-shadow:inset 0 .24rem .4rem rgba(49,33,56,.22),0 .12rem .28rem rgba(8,5,10,.22)}.mmv-choice.correct{border-color:#548c71;background:radial-gradient(circle at 35% 20%,#f3fff8,#cdebdc 57%,#86b69e);color:#285d46}.mmv-choice.wrong{border-color:#a44a5e;background:radial-gradient(circle at 35% 20%,#fff5f7,#f0cbd3 57%,#cf8393);color:#772f42}.mmv-choice:disabled{opacity:1;cursor:default}.mmv-status{position:relative;z-index:1;min-height:1.5rem;margin:.52rem .1rem 0;color:#eee7f1;text-align:center;font-size:.72rem;font-weight:900;line-height:1.35}.mmv-stage.timeout .mmv-console{filter:saturate(.52)}.mmv-stage[data-reduced=true] *{transition:none!important;animation:none!important}@media(prefers-reduced-motion:reduce){.mmv-stage *{transition:none!important;animation:none!important}}@media(max-width:395px){.mmv-item{min-height:3.85rem}.mmv-row{gap:.3rem}}
`;
function render(task,context){
 const issues=validate(task);if(issues.length)throw new Error(`${metadata.id}: ${issues.join("; ")}`);const doc=context.host?.ownerDocument;if(!doc?.createElement)throw new TypeError(`${metadata.id}: context.host.ownerDocument is required`);const make=(tag,className,text)=>{const node=doc.createElement(tag);if(className)node.className=className;if(text!==undefined)node.textContent=text;return node},externalStatus=context.host.closest?.(".question-card")?.querySelector?.(".question-help")||null,style=make("style"),stage=make("section","mmv-stage flash"),consoleNode=make("div","mmv-console"),label=make("p","mmv-label","OBSERVATION TRAY"),tray=make("div","mmv-tray"),row=make("div","mmv-row"),phaseCopy=make("p","mmv-phase",""),choices=make("div","mmv-choices"),status=make("p","mmv-status","");style.textContent=CSS;stage.tabIndex=0;stage.dataset.reduced=String(Boolean(context.reducedMotion));stage.setAttribute("role","application");stage.setAttribute("aria-label","なかったものを思い出す問題");row.setAttribute("role","list");row.setAttribute("aria-label","覚える5つ");phaseCopy.setAttribute("role","status");phaseCopy.setAttribute("aria-live","polite");choices.hidden=true;choices.setAttribute("role","group");choices.setAttribute("aria-label","答えの候補");status.setAttribute("role","status");status.setAttribute("aria-live","polite");if(externalStatus)externalStatus.textContent=task.help;const cells=task.shown.map((value,index)=>{const cell=make("span","mmv-item",value);cell.setAttribute("role","listitem");cell.setAttribute("aria-label",`${index+1}番目 ${value}`);row.append(cell);return cell}),buttons=task.options.map((value,index)=>{const button=make("button","mmv-choice",value);button.type="button";button.disabled=true;button.dataset.index=String(index);button.setAttribute("aria-label",`候補${index+1} ${value}`);choices.append(button);return button});tray.append(row);consoleNode.append(label,tray,phaseCopy,choices,status);stage.append(consoleNode);context.host.replaceChildren(style,stage);
 const state={phase:"flash",revealed:false,focusIndex:0,selectedIndex:null,correct:null,done:false,committed:false,disposed:false,finishCalls:0,result:null};const setHelp=text=>{phaseCopy.textContent=text;if(externalStatus)externalStatus.textContent=text};const refresh=()=>{stage.className=`mmv-stage ${state.phase}`;choices.hidden=!state.revealed;buttons.forEach((button,index)=>{button.disabled=!state.revealed||state.done;button.classList.toggle("selected",state.selectedIndex===index);button.classList.toggle("correct",state.selectedIndex===index&&state.correct===true);button.classList.toggle("wrong",state.selectedIndex===index&&state.correct===false)})};
 const commit=payload=>{if(state.committed||state.disposed)return false;state.committed=true;state.finishCalls++;context.finish(Boolean(state.correct),payload);return true};const choose=index=>{if(!state.revealed||state.done||state.disposed||!Number.isSafeInteger(index)||index<0||index>=buttons.length)return false;state.done=true;state.phase=task.options[index]===task.answer?"success":"wrong";state.selectedIndex=index;state.correct=task.options[index]===task.answer;state.result={correct:state.correct,payload:{answerLabel:task.answer}};status.textContent=state.correct?"正解":"不正解";refresh();context.later(()=>commit({answerLabel:task.answer}),FEEDBACK_MS);return true};const timeout=()=>{if(!state.revealed||state.done||state.disposed)return false;state.done=true;state.phase="timeout";state.correct=false;state.result={correct:false,payload:{detail:TIMEOUT_DETAIL,answerLabel:task.answer}};status.textContent=TIMEOUT_DETAIL;refresh();return commit({detail:TIMEOUT_DETAIL,answerLabel:task.answer})};const reveal=()=>{if(state.revealed||state.done||state.disposed)return false;state.revealed=true;state.phase="recall";cells.forEach(cell=>{cell.textContent="";cell.classList.add("covered");cell.setAttribute("aria-hidden","true");cell.removeAttribute?.("aria-label")});row.setAttribute("aria-label","5つを伏せました");setHelp(RECALL_HELP);refresh();context.setDeadline(task.duration,timeout);return true};
 buttons.forEach((button,index)=>{context.listen(button,"pointerdown",event=>{event.preventDefault();choose(index)});context.listen(button,"click",event=>{if(event.detail===0){event.preventDefault();choose(index)}});context.listen(button,"focus",()=>{state.focusIndex=index})});context.listen(choices,"keydown",event=>{if(!state.revealed||state.done)return;const active=buttons.indexOf(doc.activeElement),from=active>=0?active:state.focusIndex;let to=from;if(event.key==="ArrowLeft"&&from%2>0)to--;else if(event.key==="ArrowRight"&&from%2<1)to++;else if(event.key==="ArrowUp"&&from>=2)to-=2;else if(event.key==="ArrowDown"&&from<2)to+=2;else return;event.preventDefault();state.focusIndex=to;buttons[to].focus({preventScroll:true})});context.later(reveal,EXPOSURE_MS);stage.focus({preventScroll:true});refresh();
 const completeFeedback=()=>state.done&&!state.committed&&state.phase!=="timeout"?commit({answerLabel:task.answer}):false;const qaApi={reveal,choose,timeout,completeFeedback,focus:index=>{if(!state.revealed||state.done||!Number.isSafeInteger(index)||index<0||index>=buttons.length)return false;state.focusIndex=index;buttons[index].focus({preventScroll:true});return true},inspect:()=>({phase:state.phase,reduced:stage.dataset.reduced==="true",revealed:state.revealed,focusIndex:state.focusIndex,selectedIndex:state.selectedIndex,selectedValue:state.selectedIndex===null?null:task.options[state.selectedIndex],correct:state.correct,done:state.done,committed:state.committed,disposed:state.disposed,finishCalls:state.finishCalls,result:state.result?structuredClone(state.result):null,disabled:buttons.filter(button=>button.disabled).length,focused:buttons.indexOf(doc.activeElement),classes:buttons.map(button=>button.className),shownValues:[...task.shown],visibleShown:cells.map(cell=>cell.textContent),covered:cells.filter(cell=>cell.classList.contains("covered")).length,choicesHidden:choices.hidden,optionText:buttons.map(button=>button.textContent),recallCopy:phaseCopy.textContent,externalHelp:externalStatus?.textContent??null,status:status.textContent,taskJSON:JSON.stringify(task),exposureMs:EXPOSURE_MS,deadlineMs:DURATION}),elements:{stage,tray,row,cells,phaseCopy,choices,buttons,status}};if(context.qa&&typeof context.qa==="object")context.qa[metadata.id]=qaApi;context.listen(context.signal,"abort",()=>{state.disposed=true;state.done=true;if(context.qa?.[metadata.id]===qaApi)delete context.qa[metadata.id]},{once:true});
}
export default Object.freeze({metadata,generate,validate,render});

const PROMPT="違うものをひとつ探して";
const HELP="似ているだけで、同じではありません。";
const DURATION=6500,SIZE=25,COLUMNS=5;
const PAIRS=Object.freeze([Object.freeze(["😀","😃"]),Object.freeze(["🐶","🐕"]),Object.freeze(["🍎","🍅"]),Object.freeze(["6","9"])]);
const metadata=Object.freeze({id:"attention-search-v1",introducedIn:"1.0",tier:2,flavor:"satisfying",step:1,family:"attention-search",category:"attention"});
const exactKeys=(value,keys)=>value&&typeof value==="object"&&!Array.isArray(value)&&Object.keys(value).length===keys.length&&keys.every(key=>Object.hasOwn(value,key));
const pairIndex=(normal,odd)=>PAIRS.findIndex(pair=>pair[0]===normal&&pair[1]===odd);
function generate({pick,randomInt}={}){if(typeof pick!=="function")throw new TypeError(`${metadata.id}: pick is required`);if(typeof randomInt!=="function")throw new TypeError(`${metadata.id}: randomInt is required`);const pair=pick(PAIRS);if(!Array.isArray(pair)||pairIndex(pair[0],pair[1])<0)throw new TypeError(`${metadata.id}: pick must return a published pair`);const oddIndex=randomInt(0,24);if(!Number.isInteger(oddIndex)||oddIndex<0||oddIndex>24)throw new RangeError(`${metadata.id}: randomInt must return an integer from 0 to 24`);return{kind:"oddGrid",prompt:PROMPT,help:HELP,normal:pair[0],odd:pair[1],oddIndex,duration:DURATION}}
function validate(task){const issues=[];if(!task||typeof task!=="object")return["task must be an object"];if(!exactKeys(task,["kind","prompt","help","normal","odd","oddIndex","duration"]))issues.push("task must retain the exact published seven-key shape");if(task.kind!=="oddGrid")issues.push("kind must remain oddGrid");if(task.prompt!==PROMPT)issues.push("prompt changed");if(task.help!==HELP)issues.push("help changed");if(pairIndex(task.normal,task.odd)<0)issues.push("normal/odd must remain one published ordered pair");if(!Number.isInteger(task.oddIndex)||task.oddIndex<0||task.oddIndex>=SIZE)issues.push("oddIndex must be an integer from 0 to 24");if(task.duration!==DURATION)issues.push(`duration must remain ${DURATION}ms`);return issues}
const STYLE=`
.ats-stage{box-sizing:border-box;width:100%;max-width:430px;min-height:clamp(34rem,calc(100svh - 11.5rem),46rem);margin-inline:auto;display:grid;grid-template-rows:auto auto auto auto 1fr;gap:.48rem;color:#302b36;outline:none;contain:layout paint}.ats-stage:focus-visible{outline:4px solid #72458a;outline-offset:3px;border-radius:1rem}.ats-head{display:grid;grid-template-columns:auto 1fr auto;align-items:center;gap:.45rem;padding:.44rem .58rem;border:1px solid #d9cedd;border-radius:.76rem;background:linear-gradient(135deg,#fffdf8,#f2eaf5);box-shadow:0 5px 16px rgba(63,43,68,.09)}.ats-brand{font:950 .68rem/1.1 system-ui;color:#573561;letter-spacing:.075em;white-space:nowrap}.ats-line{height:2px;border-radius:999px;background:linear-gradient(90deg,transparent,#9d73af 18%,#d1b7da 50%,#9d73af 82%,transparent)}.ats-count{padding:.25rem .46rem;border-radius:999px;background:#392542;color:#fff;font:950 .66rem/1 system-ui;white-space:nowrap}.ats-board{position:relative;padding:.68rem;border:1px solid #8f7798;border-radius:1.08rem;background:radial-gradient(circle at 50% 0,rgba(255,255,255,.9),rgba(244,236,246,.92) 45%,rgba(222,211,228,.96));box-shadow:0 13px 28px rgba(51,38,58,.2),inset 0 0 0 1px rgba(255,255,255,.72);overflow:hidden}.ats-board::before{content:"";position:absolute;inset:0;pointer-events:none;background:repeating-linear-gradient(0deg,transparent 0 22px,rgba(107,79,118,.032) 23px 24px)}.ats-grid{position:relative;z-index:1;display:grid;grid-template-columns:repeat(5,minmax(0,1fr));gap:.38rem}.ats-cell{position:relative;display:grid;place-items:center;min-width:0;aspect-ratio:1;border:1px solid #cfc2d4;border-radius:.7rem;background:linear-gradient(145deg,#fffefd,#f5eff7 62%,#e5dce9);box-shadow:0 4px 8px rgba(59,45,64,.13),inset 0 1px 0 rgba(255,255,255,.95);color:#27222c;font:900 clamp(1.26rem,6.5vw,1.72rem)/1 "Apple Color Emoji","Segoe UI Emoji",system-ui,sans-serif;cursor:pointer;touch-action:manipulation;user-select:none}.ats-cell:hover{border-color:#ad91ba}.ats-cell:focus-visible{z-index:2;outline:4px solid #75468e;outline-offset:1px;box-shadow:0 0 0 7px rgba(117,70,142,.17),0 5px 11px rgba(50,35,57,.18)}.ats-cell:disabled{opacity:1;color:#27222c}.ats-cell[data-selected=true]{transform:translateY(1px) scale(.96);border-color:#d2aa4f;background:linear-gradient(#fff8d6,#f3df9b);box-shadow:inset 0 3px 7px rgba(102,76,22,.15)}.ats-stage[data-phase=CHECK] .ats-cell[data-selected=true]{box-shadow:0 0 0 5px rgba(213,171,66,.24),inset 0 3px 7px rgba(102,76,22,.15)}.ats-stage[data-result=success] .ats-cell[data-selected=true]{border-color:#43835a;background:linear-gradient(#e9f8e9,#badebf);box-shadow:0 0 0 5px rgba(66,137,83,.22)}.ats-stage[data-result=wrong] .ats-cell[data-selected=true]{border-color:#ae4e57;background:linear-gradient(#fdebea,#efbbb9);box-shadow:0 0 0 5px rgba(174,78,87,.2)}.ats-stage[data-result=timeout] .ats-grid{filter:saturate(.55);opacity:.68}.ats-banner{position:absolute;z-index:4;left:12%;right:12%;top:50%;transform:translateY(-50%);padding:.82rem .65rem;border:2px solid rgba(255,255,255,.78);border-radius:1rem;color:#fff;text-align:center;box-shadow:0 12px 28px rgba(40,27,45,.28);font:950 clamp(1rem,4.5vw,1.24rem)/1.35 system-ui}.ats-banner[hidden]{display:none}.ats-stage[data-result=success] .ats-banner{background:rgba(46,112,67,.95)}.ats-stage[data-result=wrong] .ats-banner{background:rgba(145,55,66,.95)}.ats-stage[data-result=timeout] .ats-banner{background:rgba(83,58,88,.95)}.ats-status{min-height:2.7rem;margin:0;padding:.49rem .65rem;border:1px solid #d8cfdd;border-radius:.72rem;background:#fff;color:#5c5261;text-align:center;font:850 .74rem/1.4 system-ui}.ats-stage[data-phase=CONTACT] .ats-status,.ats-stage[data-phase=CHECK] .ats-status{border-color:#dbc077;background:#fff7dc;color:#6e541c}.ats-stage[data-result=success] .ats-status{border-color:#83b992;background:#e4f4e7;color:#2b633c}.ats-stage[data-result=wrong] .ats-status{border-color:#cf8587;background:#fbe7e5;color:#7c383c}.ats-stage[data-result=timeout] .ats-status{border-color:#a78aa9;background:#f1e8f1;color:#604561}.ats-hint{margin:0;color:#756d78;text-align:center;font:750 .63rem/1.35 system-ui}.ats-scan{align-self:stretch;align-content:center;display:grid;gap:.58rem;min-height:5.5rem;padding:.72rem .66rem;border:1px solid #ded3e2;border-radius:.78rem;background:radial-gradient(circle at 16% 28%,rgba(151,111,168,.12) 0 3px,transparent 4px),radial-gradient(circle at 84% 72%,rgba(151,111,168,.1) 0 3px,transparent 4px),repeating-radial-gradient(ellipse at 50% 50%,transparent 0 22px,rgba(139,104,154,.045) 23px 25px),linear-gradient(145deg,rgba(255,255,255,.84),rgba(238,230,242,.84));color:#665b6c}.ats-scan-head{display:flex;align-items:center;justify-content:space-between;gap:.6rem;font:900 .61rem/1.2 system-ui;letter-spacing:.06em}.ats-scan-head span:last-child{color:#866594}.ats-scan-rows{display:grid;grid-template-columns:repeat(5,1fr);gap:.3rem}.ats-scan-row{height:.36rem;border-radius:999px;background:linear-gradient(90deg,#cdb9d5,#8d6b9c,#cdb9d5);box-shadow:inset 0 1px 1px rgba(255,255,255,.75)}.ats-scan-copy{margin:0;text-align:center;color:#7a7080;font:750 .59rem/1.35 system-ui}.ats-stage[data-reduced=true] *{animation:none!important;transition:none!important}@media(prefers-reduced-motion:reduce){.ats-stage *{animation:none!important;transition:none!important}}
`;
function render(task,context){
  const issues=validate(task);if(issues.length)throw new Error(`${metadata.id}: ${issues.join("; ")}`);const documentRef=context.host?.ownerDocument;if(!documentRef?.createElement)throw new TypeError(`${metadata.id}: context.host.ownerDocument is required`);const view=documentRef.defaultView||globalThis,reduced=Boolean(context.reducedMotion),style=documentRef.createElement("style"),stage=documentRef.createElement("section"),head=documentRef.createElement("header"),brand=documentRef.createElement("strong"),line=documentRef.createElement("span"),count=documentRef.createElement("span"),board=documentRef.createElement("div"),grid=documentRef.createElement("div"),banner=documentRef.createElement("div"),status=documentRef.createElement("p"),hint=documentRef.createElement("p"),scan=documentRef.createElement("footer"),scanHead=documentRef.createElement("div"),scanLabel=documentRef.createElement("span"),scanCount=documentRef.createElement("span"),scanRows=documentRef.createElement("div"),scanCopy=documentRef.createElement("p");style.textContent=STYLE;stage.className="ats-stage";stage.dataset.phase="INITIAL";stage.dataset.result="";stage.dataset.reduced=String(reduced);stage.tabIndex=0;stage.setAttribute("role","group");stage.setAttribute("aria-label","5行5列、25個の中から違うものをひとつ探します");head.className="ats-head";brand.className="ats-brand";brand.textContent="VISUAL SEARCH";line.className="ats-line";count.className="ats-count";count.textContent="25 CELLS";head.append(brand,line,count);board.className="ats-board";grid.className="ats-grid";grid.setAttribute("role","group");grid.setAttribute("aria-label","5行5列の探索グリッド");banner.className="ats-banner";banner.hidden=true;banner.setAttribute("role","status");board.append(grid,banner);status.className="ats-status";status.setAttribute("role","status");status.setAttribute("aria-live","polite");status.textContent="25個を順に見て、違うものをひとつ選びます";hint.className="ats-hint";hint.textContent="セルをタップ / 矢印キーで5×5移動 / Enter・Space";scan.className="ats-scan";scanHead.className="ats-scan-head";scanLabel.textContent="SCAN ROUTE";scanCount.textContent="ROW 1 → 5";scanHead.append(scanLabel,scanCount);scanRows.className="ats-scan-rows";for(let row=0;row<5;row++){const bar=documentRef.createElement("span");bar.className="ats-scan-row";bar.setAttribute("aria-hidden","true");scanRows.append(bar)}scanCopy.className="ats-scan-copy";scanCopy.textContent="端から端へ、同じ順序で見比べます";scan.append(scanHead,scanRows,scanCopy);stage.append(head,board,status,hint,scan);context.host.replaceChildren(style,stage);
  const cells=[];let token=0,staticScene=false;const state={phase:"INITIAL",focusIndex:0,selectedIndex:null,busy:false,done:false,disposed:false,result:null,detail:"",correct:null,stages:0,finishCalls:0};
  const glyphAt=index=>index===task.oddIndex?task.odd:task.normal;
  const update=()=>{stage.dataset.phase=state.phase;stage.dataset.result=state.result||"";stage.dataset.busy=String(state.busy);banner.hidden=!state.result;banner.textContent=state.detail;for(let index=0;index<cells.length;index++){const cell=cells[index];cell.tabIndex=index===state.focusIndex?0:-1;cell.disabled=state.busy||state.done;cell.dataset.selected=String(index===state.selectedIndex);cell.setAttribute("aria-pressed",String(index===state.selectedIndex))}};
  const finish=(correct,detail,result)=>{state.finishCalls++;if(state.done||state.disposed)return false;state.done=true;state.busy=false;state.correct=correct;state.result=result;state.detail=detail;state.phase=result.toUpperCase();status.textContent=detail;update();return context.finish(correct,{detail})};
  const settleSelection=()=>{if(state.done||state.disposed||state.selectedIndex===null)return false;const correct=state.selectedIndex===task.oddIndex;return finish(correct,correct?"見つかってしまいました。":"そっくりさんでした。",correct?"success":"wrong")};
  const choose=index=>{if(state.done||state.disposed||state.busy||!Number.isInteger(index)||index<0||index>=SIZE)return false;token++;const activeToken=token;state.selectedIndex=index;state.focusIndex=index;state.busy=true;state.phase="CONTACT";status.textContent=`${index+1}番目を確認しています`;update();if(reduced){context.later(()=>{if(activeToken===token&&!state.done&&!state.disposed){state.stages++;state.phase="CHECK";status.textContent=`${index+1}番目を照合しました`;update()}},120);context.later(()=>{if(activeToken===token&&!state.done&&!state.disposed){state.stages++;settleSelection()}},260)}else context.later(()=>{if(activeToken===token&&!state.done&&!state.disposed){state.stages++;settleSelection()}},160);return true};
  const move=(dx,dy)=>{if(state.done||state.disposed||state.busy)return false;const row=Math.floor(state.focusIndex/COLUMNS),column=state.focusIndex%COLUMNS,nextRow=Math.max(0,Math.min(COLUMNS-1,row+dy)),nextColumn=Math.max(0,Math.min(COLUMNS-1,column+dx));state.focusIndex=nextRow*COLUMNS+nextColumn;cells[state.focusIndex]?.focus({preventScroll:true});status.textContent=`${state.focusIndex+1}番目に移動`;update();return true};
  for(let index=0;index<SIZE;index++){const cell=documentRef.createElement("button");cell.type="button";cell.className="ats-cell";cell.textContent=glyphAt(index);cell.setAttribute("aria-label",`${index+1}番目 ${cell.textContent}`);context.listen(cell,"pointerdown",event=>{event.preventDefault();choose(index)});context.listen(cell,"click",event=>{if(event.detail===0)choose(index)});context.listen(cell,"focus",()=>{if(!state.busy&&!state.done){state.focusIndex=index;update()}});cells.push(cell);grid.append(cell)}
  context.listen(stage,"keydown",event=>{if(event.key==="ArrowLeft"||event.key==="ArrowRight"||event.key==="ArrowUp"||event.key==="ArrowDown"){event.preventDefault();move(event.key==="ArrowLeft"?-1:event.key==="ArrowRight"?1:0,event.key==="ArrowUp"?-1:event.key==="ArrowDown"?1:0)}else if(event.key==="Enter"||event.key===" "){event.preventDefault();choose(state.focusIndex)}});if(view&&typeof view.dispatchEvent==="function")context.listen(view,"resize",()=>update(),{passive:true});
  const expire=()=>{token++;return finish(false,"違うものは群衆に紛れました。","timeout")};context.setDeadline(task.duration,expire);
  const resetScene=()=>{token++;Object.assign(state,{phase:"INITIAL",focusIndex:0,selectedIndex:null,busy:false,done:false,result:null,detail:"",correct:null});banner.hidden=true;status.textContent="25個を順に見て、違うものをひとつ選びます"};
  const firstNormal=()=>task.oddIndex===0?1:0;
  const showScene=name=>{if(state.disposed)return false;staticScene=true;resetScene();if(name==="initial"){stage.focus({preventScroll:true})}else if(name==="focus"){state.phase="FOCUS";state.focusIndex=(task.oddIndex+1)%SIZE;status.textContent=`${state.focusIndex+1}番目にキーボードフォーカス`;cells[state.focusIndex].focus({preventScroll:true})}else if(name==="wrong"){state.selectedIndex=firstNormal();state.focusIndex=state.selectedIndex;state.done=true;state.result="wrong";state.correct=false;state.detail="そっくりさんでした。";state.phase="WRONG";status.textContent=state.detail}else if(name==="success"){state.selectedIndex=task.oddIndex;state.focusIndex=task.oddIndex;state.done=true;state.result="success";state.correct=true;state.detail="見つかってしまいました。";state.phase="SUCCESS";status.textContent=state.detail}else if(name==="timeout"){state.done=true;state.result="timeout";state.correct=false;state.detail="違うものは群衆に紛れました。";state.phase="TIMEOUT";status.textContent=state.detail}else return false;update();return true};
  const inspect=()=>({phase:state.phase,focusIndex:state.focusIndex,selectedIndex:state.selectedIndex,busy:state.busy,done:state.done,disposed:state.disposed,result:state.result,detail:state.detail,correct:state.correct,stages:state.stages,frames:0,finishCalls:state.finishCalls,pairIndex:pairIndex(task.normal,task.odd),oddIndex:task.oddIndex,cellCount:cells.length,oddCount:cells.filter(cell=>cell.textContent===task.odd).length,glyphs:cells.map(cell=>cell.textContent),arias:cells.map(cell=>cell.getAttribute("aria-label")),dpr:Math.min(3,Math.max(1,Number(context.viewport?.dpr||view.devicePixelRatio||1))),status:status.textContent,reduced});
  const qaApi={choose,move,expire,showScene,inspect};update();stage.focus({preventScroll:true});if(context.qa&&typeof context.qa==="object")context.qa[metadata.id]=qaApi;context.listen(context.signal,"abort",()=>{state.disposed=true;state.done=true;state.busy=false;token++;if(context.qa?.[metadata.id]===qaApi)delete context.qa[metadata.id]},{once:true});
}
export default Object.freeze({metadata,generate,validate,render});

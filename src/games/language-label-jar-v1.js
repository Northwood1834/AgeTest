const DURATION=20000,PROMPT="見本のラベルを、同じ絵のびんへ",HELP="ラベルをドラッグ。2つの絵のびんには2枚。";
const metadata=Object.freeze({id:"language-label-jar-v1",introducedIn:"2.0",tier:2,flavor:"satisfying",step:1,family:"language-label-jar",category:"language"});
const VARIANTS=Object.freeze([
 Object.freeze({id:"amber-pantry",labels:Object.freeze(["MIRA","SENO","PALA"]),palette:Object.freeze([Object.freeze(["#d39a45","#f2d486"]),Object.freeze(["#708b58","#b7c887"]),Object.freeze(["#a6534d","#df8c76"])]),start:Object.freeze(["g2","b1","l1","g1","b2"])}),
 Object.freeze({id:"cream-pantry",labels:Object.freeze(["NEMA","TORI","LUSO"]),palette:Object.freeze([Object.freeze(["#b9854f","#ead1a2"]),Object.freeze(["#5f876c","#a9c6a7"]),Object.freeze(["#9d5c73","#d89bae"])]),start:Object.freeze(["b2","g1","b1","l1","g2"])}),
 Object.freeze({id:"rose-pantry",labels:Object.freeze(["VENA","KIMO","RAPA"]),palette:Object.freeze([Object.freeze(["#c08b55","#f0c98a"]),Object.freeze(["#70816a","#b8c49a"]),Object.freeze(["#a14e5d","#dc8790"])]),start:Object.freeze(["l1","b2","g2","b1","g1"])})
]);
const ARTS=Object.freeze(["grain","leaf","berry"]),LABEL_RULES=Object.freeze([Object.freeze({id:"g1",ingredient:"grain"}),Object.freeze({id:"l1",ingredient:"leaf"}),Object.freeze({id:"b1",ingredient:"berry"}),Object.freeze({id:"g2",ingredient:"grain"}),Object.freeze({id:"b2",ingredient:"berry"})]),JAR_RULES=Object.freeze([Object.freeze({id:"grain-jar",ingredients:Object.freeze(["grain"])}),Object.freeze({id:"leaf-jar",ingredients:Object.freeze(["leaf"])}),Object.freeze({id:"berry-jar",ingredients:Object.freeze(["berry"])}),Object.freeze({id:"blend-jar",ingredients:Object.freeze(["grain","berry"])})]);
const clone=value=>JSON.parse(JSON.stringify(value)),sameJson=(a,b)=>JSON.stringify(a)===JSON.stringify(b),exactKeys=(value,keys)=>value&&typeof value==="object"&&!Array.isArray(value)&&sameJson(Object.keys(value).sort(),[...keys].sort()),permutation=(values,expected)=>Array.isArray(values)&&values.length===expected.length&&new Set(values).size===expected.length&&values.every(value=>expected.includes(value));
const permutations=values=>{const out=[],work=[...values];const visit=index=>{if(index===work.length){out.push([...work]);return}for(let next=index;next<work.length;next++){[work[index],work[next]]=[work[next],work[index]];visit(index+1);[work[index],work[next]]=[work[next],work[index]]}};visit(0);return out};
const slotIngredients=jars=>jars.flatMap(jar=>jar.ingredients),labelById=labels=>new Map(labels.map(label=>[label.id,label]));
function analyzeLayout(labels,jars,slots){const byId=labelById(labels),requirements=slotIngredients(jars),mismatches=[];let filled=0;for(let index=0;index<requirements.length;index++){const id=slots[index];if(id){filled++;if(byId.get(id)?.ingredient!==requirements[index])mismatches.push(index)}}return{filled,total:requirements.length,mismatches,full:filled===requirements.length,success:filled===requirements.length&&!mismatches.length,compoundFilled:slots.slice(-2).filter(Boolean).length}}
function proofFor(labels,jars){const arrangements=permutations(labels.map(label=>label.id)),valid=arrangements.filter(slots=>analyzeLayout(labels,jars,slots).success).length;return{totalFullArrangements:arrangements.length,validFullArrangements:valid,invalidFullArrangements:arrangements.length-valid,placementOrders:120,everyMoveReversible:true}}
function authoredTask(variant){const ingredients=ARTS.map((id,index)=>({id,label:variant.labels[index],art:id,base:variant.palette[index][0],accent:variant.palette[index][1]})),labels=LABEL_RULES.map(rule=>({id:rule.id,ingredient:rule.ingredient,text:ingredients.find(item=>item.id===rule.ingredient).label})),jars=JAR_RULES.map(jar=>({id:jar.id,ingredients:[...jar.ingredients]}));return{kind:"labelJar",prompt:PROMPT,help:HELP,variantId:variant.id,ingredients,jars,labels,start:[...variant.start],proof:proofFor(labels,jars),duration:DURATION,state:{slots:Array(5).fill(null),tray:[...variant.start],held:null,moves:0}}}
function generate({pick}={}){let selected;try{selected=typeof pick==="function"?pick(VARIANTS.map(row=>({id:row.id}))):null}catch{selected=null}const variant=VARIANTS.find(row=>row.id===selected?.id)||VARIANTS[0];return authoredTask(variant)}
const validSnapshot=(value,labelIds)=>exactKeys(value,["slots","tray"])&&Array.isArray(value.slots)&&value.slots.length===5&&value.slots.every(id=>id===null||labelIds.includes(id))&&Array.isArray(value.tray)&&value.tray.every(id=>labelIds.includes(id))&&permutation([...value.slots.filter(Boolean),...value.tray],labelIds);
function validState(state,labelIds){if(!exactKeys(state,["slots","tray","held","moves"])||!Number.isSafeInteger(state.moves)||state.moves<0||!Array.isArray(state.slots)||state.slots.length!==5||!Array.isArray(state.tray))return false;const held=state.held,inventory=[...state.slots.filter(Boolean),...state.tray];if(held!==null){if(!exactKeys(held,["id","source","index","before"])||!labelIds.includes(held.id)||!["tray","slot"].includes(held.source)||!Number.isSafeInteger(held.index)||!validSnapshot(held.before,labelIds))return false;inventory.push(held.id);const expected=clone(held.before);if(held.source==="tray"){if(held.index<0||held.index>=expected.tray.length||expected.tray[held.index]!==held.id)return false;expected.tray.splice(held.index,1)}else{if(held.index<0||held.index>=5||expected.slots[held.index]!==held.id)return false;expected.slots[held.index]=null}if(!sameJson(state.slots,expected.slots)||!sameJson(state.tray,expected.tray))return false}return permutation(inventory,labelIds)}
function validate(task){if(!task||typeof task!=="object"||Array.isArray(task))return["task must be a plain labelJar object"];const issues=[];if(!exactKeys(task,["kind","prompt","help","variantId","ingredients","jars","labels","start","proof","duration","state"]))issues.push("task fields changed");if(task.kind!=="labelJar")issues.push("kind must remain labelJar");if(task.prompt!==PROMPT||task.help!==HELP)issues.push("copy changed");if(task.duration!==DURATION)issues.push("duration must remain 20000ms");const variant=VARIANTS.find(row=>row.id===task.variantId);if(!variant){issues.push("variantId must remain authored");return issues}const expected=authoredTask(variant);for(const key of["ingredients","jars","labels","start","proof"])if(!sameJson(task[key],expected[key]))issues.push(`${key} changed`);const labelIds=expected.labels.map(label=>label.id);if(!validState(task.state,labelIds))issues.push("state must preserve the exact finite label inventory and held source");return[...new Set(issues)]}
const STYLE=`
.llj-stage{box-sizing:border-box;width:100%;max-width:430px;margin:auto;display:grid;gap:.42rem;color:#49382c;font-family:"Hiragino Kaku Gothic ProN","Yu Gothic",system-ui,sans-serif}.llj-stage *{box-sizing:border-box}.llj-topline{display:flex;align-items:center;justify-content:space-between;gap:.5rem}.llj-heading{font-size:.72rem;font-weight:950;letter-spacing:.08em}.llj-time{min-width:5.5rem;padding:.3rem .55rem;border:1px solid #d4b390;border-radius:999px;background:#fff8ec;text-align:center;font-size:.78rem;font-weight:950}.llj-time.low{border-color:#d17b74;background:#fff0ed;color:#8e3535}.llj-legend{display:grid;grid-template-columns:repeat(3,1fr);gap:.34rem}.llj-legend-card{position:relative;min-height:4.55rem;padding:.32rem .25rem .25rem;border:1px solid #d8bea0;border-radius:.72rem;background:linear-gradient(145deg,#fffdf7,#f4e2ca);box-shadow:0 4px 9px rgba(85,56,36,.12);display:grid;grid-template-columns:2.8rem 1fr;align-items:center}.llj-legend-card::before{content:"";position:absolute;left:50%;top:-.15rem;width:1.3rem;height:.34rem;transform:translateX(-50%);border-radius:.15rem;background:#b87645;box-shadow:inset 0 1px rgba(255,255,255,.45)}.llj-legend-text{font-size:.78rem;font-weight:1000;letter-spacing:.04em;text-align:center}.llj-pantry{position:relative;min-height:24rem;padding:2.25rem .42rem 3.2rem;overflow:hidden;border:3px solid #795239;border-radius:1.05rem;background:linear-gradient(90deg,rgba(255,239,207,.42),rgba(255,255,255,.65) 50%,rgba(238,205,164,.4)),linear-gradient(#ead3b5,#d5ad80);box-shadow:inset 0 0 0 3px #f4e1c7,0 10px 22px rgba(78,48,31,.2);touch-action:none}.llj-pantry::before{content:"";position:absolute;left:5%;right:5%;top:.62rem;height:1rem;border-radius:.2rem;background:repeating-linear-gradient(90deg,#b57a48 0 18px,#c78b55 18px 36px);box-shadow:0 .42rem 0 #704a34,0 .68rem .8rem rgba(69,43,28,.22)}.llj-pantry::after{content:"";position:absolute;z-index:1;left:12%;top:0;width:28%;height:88%;transform:skewX(-10deg);background:linear-gradient(110deg,rgba(255,250,213,.55),rgba(255,220,158,0));pointer-events:none}.llj-jars{position:relative;z-index:3;display:grid;grid-template-columns:repeat(6,1fr);gap:.38rem}.llj-jar{position:relative;grid-column:span 2;min-height:10.35rem;padding:.52rem .28rem .42rem;border-radius:.75rem;background:rgba(255,248,235,.26);display:grid;grid-template-rows:5.8rem 1fr;align-items:end}.llj-jar.compound{grid-column:2/span 4;min-height:8.3rem;grid-template-rows:4.2rem 1fr;margin-top:.15rem;padding-inline:.8rem}.llj-glass{position:relative;align-self:end;width:82%;height:5.65rem;margin:auto;border:3px solid rgba(119,83,59,.68);border-top-width:7px;border-radius:.65rem .65rem 1rem 1rem;background:linear-gradient(100deg,rgba(255,255,255,.68),rgba(255,247,223,.28) 42%,rgba(227,198,160,.38));box-shadow:inset .35rem 0 rgba(255,255,255,.34),inset -.28rem 0 rgba(128,91,63,.09),0 .45rem .55rem rgba(74,44,28,.2);display:flex;align-items:flex-end;justify-content:space-evenly;padding:.35rem}.llj-glass::before{content:"";position:absolute;left:8%;right:8%;top:-.65rem;height:.52rem;border:3px solid #805b43;border-radius:.35rem;background:repeating-linear-gradient(90deg,#b47745 0 7px,#c98b55 7px 14px)}.llj-jar.compound .llj-glass{width:72%;height:4.1rem}.llj-jar.compound .llj-glass::after{content:"";position:absolute;left:50%;top:.35rem;bottom:.28rem;width:2px;background:rgba(108,74,50,.25)}.llj-slots{display:grid;grid-template-columns:repeat(var(--slots),minmax(0,1fr));gap:.28rem;margin-top:.28rem}.llj-slot{position:relative;z-index:8;min-width:0;min-height:3.05rem;border:3px solid #9a7458;border-radius:.58rem;background:rgba(255,252,244,.78);box-shadow:inset 0 2px #fff,0 3px 6px rgba(78,49,31,.13);display:grid;place-items:center;padding:.14rem;touch-action:none}.llj-slot.empty{border-style:dashed;background:rgba(255,251,241,.52)}.llj-slot.empty::before{content:"ラベル";color:#8a725e;font-size:.62rem;font-weight:950}.llj-slot.mismatch{border-color:#c27943;background:#fff3dd;box-shadow:0 0 0 3px rgba(196,119,64,.18)}.llj-slot.mismatch::after{content:"絵を確認";position:absolute;right:.12rem;bottom:.08rem;padding:.06rem .2rem;border-radius:999px;background:#b76f3d;color:#fff;font-size:.46rem;font-weight:950}.llj-slot.target{border-color:#6d4f7d;box-shadow:0 0 0 4px #fff,0 0 0 7px #795a88}.llj-slot:focus-visible,.llj-label:focus-visible{outline:4px solid #674879;outline-offset:3px}.llj-tray-wrap{position:relative;z-index:4;margin-top:.48rem;padding:.38rem;border:2px solid rgba(119,78,50,.38);border-radius:.78rem;background:rgba(255,249,237,.68)}.llj-tray-title{display:block;margin-bottom:.25rem;font-size:.66rem;font-weight:950}.llj-tray{min-height:3.15rem;display:grid;grid-template-columns:repeat(5,1fr);gap:.25rem}.llj-label{position:relative;min-width:0;min-height:2.8rem;padding:.34rem .15rem;border:2px solid #805c44;border-radius:.48rem;background:linear-gradient(#fffaf0,#ecd2aa);color:#4b382c;font-size:.7rem;font-weight:1000;letter-spacing:.02em;box-shadow:inset 0 2px #fff,0 4px 6px rgba(73,44,28,.2);touch-action:none;cursor:grab}.llj-slot .llj-label{width:100%;min-width:44px}.llj-held .llj-label{width:4.5rem;min-width:44px}.llj-label::before{content:"";position:absolute;left:20%;right:20%;top:.18rem;height:.18rem;border-radius:999px;background:repeating-linear-gradient(90deg,#9c765a 0 3px,transparent 3px 6px)}.llj-label.held{border-color:#6d4f7d;background:#f3e7f4;box-shadow:0 0 0 4px #fff,0 0 0 7px #795a88}.llj-label.dragging{position:absolute;z-index:40;width:4.5rem;min-height:3rem;transform:translate(-50%,-50%) rotate(-3deg) scale(1.08);box-shadow:0 14px 13px rgba(65,38,26,.35);pointer-events:none;cursor:grabbing}.llj-held{min-height:3.05rem;display:grid;place-items:center}.llj-held:empty{display:none}.llj-float{position:absolute;inset:0;z-index:38;pointer-events:none}.llj-note{position:relative;z-index:4;min-height:1.25rem;margin:.35rem 0 0;text-align:center;color:#87552f;font-size:.66rem;font-weight:950}.llj-note:empty{visibility:hidden}.llj-status{position:absolute;width:1px;height:1px;margin:-1px;padding:0;overflow:hidden;clip:rect(0 0 0 0);white-space:nowrap;border:0}.llj-art{--base:#c18a51;--accent:#edd0a0;position:relative;display:block;width:2.55rem;height:2.55rem;flex:0 0 auto;filter:drop-shadow(0 4px 3px rgba(64,40,28,.22))}.llj-glass .llj-art{transform:scale(.88);transform-origin:bottom}.llj-art i{position:absolute;display:block}.art-grain i{width:.72rem;height:1.18rem;border:2px solid color-mix(in srgb,var(--base),#5b3d2b 25%);border-radius:55% 45% 58% 42%;background:linear-gradient(90deg,var(--base),var(--accent))}.art-grain i:nth-child(1){left:.28rem;bottom:.12rem;transform:rotate(-22deg)}.art-grain i:nth-child(2){left:1rem;bottom:.05rem;transform:rotate(8deg)}.art-grain i:nth-child(3){right:.18rem;bottom:.25rem;transform:rotate(25deg)}.art-grain i:nth-child(4){left:.68rem;top:.08rem;transform:rotate(-8deg)}.art-grain i:nth-child(5){right:.42rem;top:.18rem;transform:rotate(19deg)}.art-leaf i:nth-child(1){inset:.12rem .28rem .2rem;border:3px solid color-mix(in srgb,var(--base),#35412e 28%);border-radius:70% 15% 70% 20%;background:linear-gradient(135deg,var(--accent),var(--base));transform:rotate(-38deg)}.art-leaf i:nth-child(2){left:1.2rem;top:.28rem;width:.16rem;height:2.25rem;border-radius:999px;background:#5f714d;transform:rotate(36deg)}.art-leaf i:nth-child(3){left:.48rem;top:1.05rem;width:1.45rem;height:.13rem;background:rgba(73,91,58,.62);transform:rotate(-4deg)}.art-berry i{width:1.15rem;height:1.15rem;border:3px solid color-mix(in srgb,var(--base),#55353a 25%);border-radius:50%;background:radial-gradient(circle at 32% 26%,var(--accent) 0 15%,var(--base) 50%)}.art-berry i:nth-child(1){left:.18rem;top:.62rem}.art-berry i:nth-child(2){right:.16rem;top:.62rem}.art-berry i:nth-child(3){left:.72rem;bottom:.1rem}.art-berry i:nth-child(4){left:.7rem;top:.02rem;width:.9rem;height:.7rem;border:0;border-radius:70% 10%;background:#71865d;transform:rotate(-18deg)}.art-berry i:nth-child(5){display:none}.llj-pantry.cross .llj-jar{opacity:.58}.llj-pantry.success{box-shadow:inset 0 0 0 3px #f4e1c7,0 0 0 5px #77936d,0 12px 28px rgba(75,105,66,.28)}.llj-pantry.success .llj-glass{background:linear-gradient(100deg,rgba(255,255,255,.85),rgba(255,244,183,.42),rgba(217,237,190,.42))}.llj-pantry.success::after{left:4%;width:90%;background:linear-gradient(110deg,rgba(255,250,185,.7),rgba(255,220,158,.08))}.llj-pantry.timeout{filter:saturate(.65)}.llj-stage[data-reduced=true] *{transition:none!important}.llj-label,.llj-jar{transition:transform .15s ease,opacity .12s ease,filter .15s ease}@media(prefers-reduced-motion:reduce){.llj-stage *{transition:none!important}}@media(max-width:405px){.llj-pantry{padding-inline:.3rem}.llj-jars{gap:.22rem}.llj-jar{padding-inline:.12rem}.llj-legend-card{grid-template-columns:2.5rem 1fr}.llj-label{font-size:.64rem}.llj-slot{min-height:2.9rem}}
`;
function render(task,context){
 const issues=validate(task);if(issues.length)throw new Error(`${metadata.id}: ${issues.join("; ")}`);const doc=context.host?.ownerDocument;if(!doc?.createElement)throw new TypeError(`${metadata.id}: context.host.ownerDocument is required`);const make=(tag,className,text)=>{const node=doc.createElement(tag);if(className)node.className=className;if(text!==undefined)node.textContent=text;return node},style=make("style"),stage=make("section","llj-stage"),topline=make("div","llj-topline"),heading=make("span","llj-heading","ラベル見本"),timeNode=make("span","llj-time"),legend=make("section","llj-legend"),pantry=make("section","llj-pantry"),jarsRoot=make("div","llj-jars"),trayWrap=make("section","llj-tray-wrap"),trayTitle=make("span","llj-tray-title","動かすラベル"),tray=make("div","llj-tray"),heldDock=make("div","llj-held"),floatLayer=make("div","llj-float"),note=make("p","llj-note"),status=make("p","llj-status");style.textContent=STYLE;stage.dataset.reduced=String(Boolean(context.reducedMotion));stage.setAttribute("role","application");stage.setAttribute("aria-label","見本と同じ絵のびんへラベルを動かすゲーム");timeNode.setAttribute("aria-label","残り時間");legend.setAttribute("aria-label","ラベルと材料の見本");pantry.setAttribute("aria-label","四つの材料びん");tray.setAttribute("aria-label","動かすラベルの置き場");status.setAttribute("role","status");status.setAttribute("aria-live","polite");topline.append(heading,timeNode);
 const byIngredient=new Map(task.ingredients.map(item=>[item.id,item])),byLabel=labelById(task.labels),makeArt=ingredient=>{const art=make("span",`llj-art art-${ingredient.art}`);art.style.setProperty("--base",ingredient.base);art.style.setProperty("--accent",ingredient.accent);art.setAttribute("aria-hidden","true");for(let index=0;index<5;index++)art.append(make("i"));return art};for(const ingredient of task.ingredients){const card=make("article","llj-legend-card"),text=make("strong","llj-legend-text",ingredient.label);card.append(makeArt(ingredient),text);legend.append(card)}
 const slotButtons=[],jarCards=[];let flatIndex=0;for(const jar of task.jars){const card=make("article",`llj-jar${jar.ingredients.length===2?" compound":""}`),glass=make("div","llj-glass"),slots=make("div","llj-slots");slots.style.setProperty("--slots",String(jar.ingredients.length));for(const ingredientId of jar.ingredients)glass.append(makeArt(byIngredient.get(ingredientId)));const indices=[];for(let local=0;local<jar.ingredients.length;local++){const slot=make("div","llj-slot");slot.tabIndex=0;slot.setAttribute("role","button");slot.dataset.index=String(flatIndex);slot.setAttribute("aria-label",`${jar.ingredients.length===2?"二つ絵のびん":"一つ絵のびん"}のラベル場所 ${local+1}`);indices.push(flatIndex);slotButtons.push(slot);slots.append(slot);flatIndex++}card.append(glass,slots);card._slotIndices=indices;jarCards.push(card);jarsRoot.append(card)}
 const labelButtons=new Map;for(const label of task.labels){const button=make("button","llj-label",label.text);button.type="button";button.setAttribute("aria-label",`ラベル ${label.text}`);labelButtons.set(label.id,button)}trayWrap.append(trayTitle,tray,heldDock);pantry.append(jarsRoot,trayWrap,floatLayer,note,status);stage.append(topline,legend,pantry);context.host.replaceChildren(style,stage);
 const state={slots:[...task.state.slots],tray:[...task.state.tray],held:task.state.held?clone(task.state.held):null,moves:task.state.moves,drag:null,target:-1,busy:false,snapStage:2,revision:0,remaining:20,done:false,disposed:false,committed:false,outcome:"active"};
 const snapshotContent=()=>({slots:[...state.slots],tray:[...state.tray]}),snapshot=()=>({slots:[...state.slots],tray:[...state.tray],held:state.held?clone(state.held):null,moves:state.moves}),analysis=()=>analyzeLayout(task.labels,task.jars,state.slots),itemLocation=id=>{const slot=state.slots.indexOf(id);if(slot>=0)return{source:"slot",index:slot};const index=state.tray.indexOf(id);return index>=0?{source:"tray",index}:null},setStatus=text=>{status.textContent=text};
 const placeNodes=()=>{for(const slot of slotButtons)slot.replaceChildren();tray.replaceChildren();heldDock.replaceChildren();floatLayer.replaceChildren();for(let index=0;index<state.slots.length;index++){const id=state.slots[index];if(id)slotButtons[index].append(labelButtons.get(id))}for(const id of state.tray)tray.append(labelButtons.get(id));if(state.held){const button=labelButtons.get(state.held.id);(state.drag?floatLayer:heldDock).append(button)}};
 const refresh=()=>{const current=analysis();timeNode.textContent=`残り ${state.remaining}秒`;timeNode.className=`llj-time${state.remaining<=5?" low":""}`;pantry.className=`llj-pantry${state.snapStage===0?" cross":""}${state.outcome!=="active"?` ${state.outcome}`:""}`;placeNodes();slotButtons.forEach((slot,index)=>{const mismatch=current.full&&current.mismatches.includes(index),isTarget=state.target===index;slot.className=`llj-slot${state.slots[index]?"":" empty"}${mismatch?" mismatch":""}${isTarget?" target":""}`;slot.setAttribute("aria-label",`${slot.getAttribute("aria-label").split("、")[0]}、${state.slots[index]?`ラベル ${byLabel.get(state.slots[index])?.text}`:"空き"}${mismatch?"、絵と違います":""}`)});labelButtons.forEach((button,id)=>{button.className=`llj-label${state.held?.id===id?" held":""}${state.drag?.id===id?" dragging":""}`;button.disabled=state.done;button.setAttribute("aria-pressed",String(state.held?.id===id));if(state.drag?.id!==id){button.style.left="";button.style.top=""}});for(const slot of slotButtons){slot.tabIndex=state.done?-1:0;slot.setAttribute("aria-disabled",String(state.done))}jarCards.forEach(card=>{card.className=`llj-jar${card._slotIndices.length===2?" compound":""}${card._slotIndices.some(index=>current.full&&current.mismatches.includes(index))?" wrong":""}`});note.textContent=current.full&&!current.success?"絵を見直して、入れ替えられます":"";pantry.setAttribute("aria-label",`四つの材料びん。${current.filled}枚配置。${current.full?current.success?"すべて一致":"絵と違う場所あり":"まだ空きあり"}`)};
 const pickLabel=id=>{if(state.done||state.busy||state.held||!byLabel.has(id))return false;const location=itemLocation(id);if(!location)return false;const before=snapshotContent();if(location.source==="tray")state.tray.splice(location.index,1);else state.slots[location.index]=null;state.held={id,source:location.source,index:location.index,before};state.target=-1;setStatus(`${byLabel.get(id).text}を持ちました。びんを選びます`);refresh();return true},cancelHeld=()=>{if(state.done||!state.held)return false;state.slots=[...state.held.before.slots];state.tray=[...state.held.before.tray];state.held=null;state.drag=null;state.target=-1;setStatus("ラベルを元へ戻しました");refresh();return true};
 const finishSuccess=()=>{if(state.disposed||state.committed||state.outcome!=="success")return false;state.committed=true;setStatus("すべてのラベルが絵と合いました");refresh();const current=analysis();context.finish(true,{outcome:"success",detail:"すべてのラベルが絵と合いました。",state:snapshot(),analysis:current});return true},settle=revision=>{if(state.done||state.disposed||revision!==state.revision)return;state.snapStage=2;state.busy=false;const current=analysis();if(current.success){state.done=true;state.outcome="success";setStatus("ラベルがびんに整いました");refresh();context.later(()=>finishSuccess(),context.reducedMotion?70:220)}else{setStatus(current.full?"絵と違う場所があります。何度でも入れ替えられます":"次のラベルを動かせます");refresh()}},scheduleSnap=()=>{state.busy=true;state.snapStage=context.reducedMotion?1:0;const revision=++state.revision;refresh();if(context.reducedMotion)context.later(()=>settle(revision),60);else{context.later(()=>{if(!state.done&&!state.disposed&&revision===state.revision){state.snapStage=1;refresh()}},80);context.later(()=>settle(revision),160)}};
 const dropHeld=index=>{if(state.done||state.busy||!state.held||!Number.isInteger(index)||index<0||index>=5)return false;const held=state.held;if(held.source==="slot"&&held.index===index)return cancelHeld();const displaced=state.slots[index];state.slots[index]=held.id;if(held.source==="slot"){state.slots[held.index]=displaced||null}else if(displaced){const insert=Math.max(0,Math.min(held.index,state.tray.length));state.tray.splice(insert,0,displaced)}state.held=null;state.drag=null;state.target=-1;state.moves++;setStatus("ラベルを入れ替えています");scheduleSnap();return true};
 const nearestSlot=(x,y)=>{let best=-1,distance=Infinity;slotButtons.forEach((slot,index)=>{const rect=slot.getBoundingClientRect(),cx=rect.left+rect.width/2,cy=rect.top+rect.height/2,next=(cx-x)**2+(cy-y)**2;if(x>=rect.left-12&&x<=rect.right+12&&y>=rect.top-12&&y<=rect.bottom+12&&next<distance){best=index;distance=next}});return best},positionDrag=(x,y)=>{if(!state.drag)return;const rect=pantry.getBoundingClientRect(),button=labelButtons.get(state.drag.id);button.style.left=`${x-rect.left}px`;button.style.top=`${y-rect.top}px`};
 const beginDrag=(id,event)=>{if(!pickLabel(id))return false;event.preventDefault();state.drag={id,pointerId:event.pointerId,startX:event.clientX,startY:event.clientY,moved:false};refresh();positionDrag(event.clientX,event.clientY);return true},dragMove=event=>{if(!state.drag||event.pointerId!==state.drag.pointerId||state.done)return;const dx=event.clientX-state.drag.startX,dy=event.clientY-state.drag.startY;if(dx*dx+dy*dy>64)state.drag.moved=true;state.target=nearestSlot(event.clientX,event.clientY);positionDrag(event.clientX,event.clientY);slotButtons.forEach((slot,index)=>slot.classList.toggle("target",state.target===index))},dragEnd=event=>{if(!state.drag||event.pointerId!==state.drag.pointerId)return;const target=nearestSlot(event.clientX,event.clientY),moved=state.drag.moved;state.drag=null;state.target=-1;if(target>=0)dropHeld(target);else if(moved)cancelHeld();else{setStatus(`${byLabel.get(state.held.id).text}を持っています。びんを選びます`);refresh()}};
 const focusControls=()=>[...slotButtons,...labelButtons.values()].filter(node=>!node.disabled&&node.parentNode),moveFocus=(node,key)=>{const controls=focusControls(),index=controls.indexOf(node);if(index<0||!controls.length)return false;const delta=key==="ArrowLeft"||key==="ArrowUp"?-1:1;controls[(index+delta+controls.length)%controls.length].focus({preventScroll:true});return true},keyAction=(node,kind,value,event)=>{if(["ArrowLeft","ArrowRight","ArrowUp","ArrowDown"].includes(event.key)){event.preventDefault();moveFocus(node,event.key)}else if(event.key===" "||event.key==="Enter"){event.preventDefault();if(kind==="label"){if(state.held?.id===value)cancelHeld();else if(!state.held)pickLabel(value)}else if(state.held)dropHeld(value);else if(state.slots[value])pickLabel(state.slots[value])}else if(event.key==="Escape"&&state.held){event.preventDefault();cancelHeld()}};
 labelButtons.forEach((button,id)=>{context.listen(button,"pointerdown",event=>{event.stopPropagation();if(!state.held)beginDrag(id,event)});context.listen(button,"click",event=>{event.stopPropagation();if(event.detail===0){event.preventDefault();state.held?.id===id?cancelHeld():!state.held&&pickLabel(id)}});context.listen(button,"keydown",event=>{event.stopPropagation();keyAction(button,"label",id,event)})});slotButtons.forEach((slot,index)=>{context.listen(slot,"pointerdown",event=>{if(state.held&&!state.drag){event.preventDefault();dropHeld(index)}});context.listen(slot,"click",event=>{if(event.detail===0){event.preventDefault();if(state.held)dropHeld(index);else if(state.slots[index])pickLabel(state.slots[index])}});context.listen(slot,"keydown",event=>keyAction(slot,"slot",index,event))});const view=doc.defaultView;if(view){context.listen(view,"pointermove",dragMove,{passive:true});context.listen(view,"pointerup",dragEnd);context.listen(view,"pointercancel",dragEnd)}context.listen(stage,"keydown",event=>{if(event.key==="Escape"&&state.held){event.preventDefault();cancelHeld()}});
 const timeout=()=>{if(state.done||state.disposed)return false;state.done=true;state.outcome="timeout";state.committed=true;state.revision++;state.busy=false;state.snapStage=2;state.remaining=0;state.drag=null;state.target=-1;const current=analysis();setStatus("時間切れ。ラベルはそのまま残ります");refresh();context.finish(false,{outcome:"timeout",detail:"時間切れ。ラベルはそのまま残ります。",state:snapshot(),analysis:current});return true},tick=()=>{if(state.done||state.disposed)return;state.remaining=Math.max(0,state.remaining-1);refresh();if(state.remaining>0)context.later(tick,1000)};context.later(tick,1000);context.setDeadline(task.duration,timeout);
 const applyLayout=(slots,tray=[],moves=state.moves)=>{if(state.done||!Array.isArray(slots)||slots.length!==5||!permutation([...slots.filter(Boolean),...tray],task.labels.map(label=>label.id)))return false;state.slots=[...slots];state.tray=[...tray];state.held=null;state.drag=null;state.target=-1;state.moves=moves;state.snapStage=2;state.revision++;refresh();return true},validArrangement=permutations(task.labels.map(label=>label.id)).find(slots=>analyzeLayout(task.labels,task.jars,slots).success),invalidArrangement=permutations(task.labels.map(label=>label.id)).find(slots=>{const current=analyzeLayout(task.labels,task.jars,slots);return current.full&&current.mismatches.length>=3}),scene=name=>{if(state.done)return false;if(name==="initial")return true;if(name==="selected")return pickLabel(state.tray[0]);if(name==="dragging"){const id=state.tray[0];if(!pickLabel(id))return false;state.drag={id,pointerId:99,startX:0,startY:0,moved:true};state.target=0;refresh();const button=labelButtons.get(id);button.style.left="34%";button.style.top="36%";return true}if(name==="partial"){const slots=[validArrangement[0],validArrangement[1],null,null,null],tray=validArrangement.slice(2);return applyLayout(slots,tray,2)}if(name==="compound-partial"){const slots=[validArrangement[0],validArrangement[1],validArrangement[2],validArrangement[3],null],tray=[validArrangement[4]];return applyLayout(slots,tray,4)}if(name==="full-mismatch")return applyLayout(invalidArrangement,[],5);if(name==="focus"){slotButtons[0].focus({preventScroll:true});return true}if(name==="success"){applyLayout(validArrangement,[],5);state.done=true;state.outcome="success";refresh();return true}return false},completeSnap=()=>{state.snapStage=2;state.busy=false;refresh();return true},completeSuccess=()=>state.outcome==="success"?finishSuccess():false;
 const qaApi={pickLabel,dropHeld,cancelHeld,applyLayout,scene,completeSnap,completeSuccess,timeout,snapshotTask:()=>({...clone(task),state:snapshot()}),elements:{stage,legend,pantry,jarsRoot,jarCards,slotButtons,tray,heldDock,floatLayer,note,status,timeNode,labelButtons},inspect:()=>{const current=analysis();return{slots:[...state.slots],tray:[...state.tray],held:state.held?clone(state.held):null,drag:state.drag?{...state.drag}:null,target:state.target,moves:state.moves,busy:state.busy,snapStage:state.snapStage,remaining:state.remaining,done:state.done,disposed:state.disposed,committed:state.committed,outcome:state.outcome,analysis:{...current,mismatches:[...current.mismatches]},note:note.textContent,status:status.textContent,focused:[...slotButtons,...labelButtons.values()].indexOf(doc.activeElement),disabled:[...slotButtons,...labelButtons.values()].filter(node=>node.disabled||node.getAttribute("aria-disabled")==="true").length}}};if(context.qa&&typeof context.qa==="object")context.qa[metadata.id]=qaApi;context.listen(context.signal,"abort",()=>{state.disposed=true;state.done=true;state.drag=null;if(context.qa?.[metadata.id]===qaApi)delete context.qa[metadata.id]},{once:true});setStatus("見本の絵を見て、ラベルを動かします");refresh();
}
export{analyzeLayout};export default Object.freeze({metadata,generate,validate,render});

const TARGETS=Object.freeze(["★","●","▲"]),SHAPES=Object.freeze(["★","●","▲","■"]),PROMPT_SUFFIX=" はいくつ？",HELP="落ち着いて数えましょう。",DURATION=8000,FEEDBACK_MS=180,TIMEOUT_DETAIL="時間切れです。脳は定時退社しました。";
const metadata=Object.freeze({id:"attention-shape-count-v1",introducedIn:"1.15",tier:1,flavor:"classic",step:1,family:"attention-shape-count",category:"attention"});
const same=(a,b)=>JSON.stringify(a)===JSON.stringify(b),exactKeys=(value,keys)=>value&&typeof value==="object"&&!Array.isArray(value)&&same(Object.keys(value).sort(),[...keys].sort()),permutation=(values,expected)=>Array.isArray(values)&&values.length===expected.length&&same([...values].sort(),[...expected].sort());
function optionSet(count){const options=[count,count+1,Math.max(0,count-1),count+2].map(String),unique=[...new Set(options)];let extra=count+3;while(unique.length<4){if(!unique.includes(String(extra)))unique.push(String(extra));extra++}return unique.slice(0,4)}
function generate(helpers={}){
 let target;try{target=helpers.pick([...TARGETS])}catch{target=null}if(!TARGETS.includes(target))target=TARGETS[0];const others=SHAPES.filter(value=>value!==target),line=[];
 for(let index=0;index<12;index++){let raw;try{raw=helpers.random()}catch{raw=1}const sample=Number(raw),hit=Number.isFinite(sample)&&sample>=0&&sample<1&&sample<.32;if(hit)line.push(target);else{let value;try{value=helpers.pick([...others])}catch{value=null}line.push(others.includes(value)?value:others[index%others.length])}}
 if(line.filter(value=>value===target).length<2){let first,second;try{first=helpers.randomInt(0,5)}catch{first=0}try{second=helpers.randomInt(6,11)}catch{second=6}if(!Number.isInteger(first)||first<0||first>5)first=0;if(!Number.isInteger(second)||second<6||second>11)second=6;line[first]=target;line[second]=target}
 const count=line.filter(value=>value===target).length,unique=optionSet(count);let shuffled;try{shuffled=helpers.shuffle([...unique])}catch{shuffled=null}const options=permutation(shuffled,unique)?[...shuffled]:[...unique];return{kind:"expression",prompt:`${target}${PROMPT_SUFFIX}`,help:HELP,expression:line.join(" "),options,answer:String(count),duration:DURATION};
}
function validate(task){
 if(!task||typeof task!=="object"||Array.isArray(task))return["task must be a plain expression object"];const issues=[];if(!exactKeys(task,["kind","prompt","help","expression","options","answer","duration"]))issues.push("task fields must remain exactly kind, prompt, help, expression, options, answer, duration");if(task.kind!=="expression")issues.push("kind must remain expression");if(task.help!==HELP)issues.push("help changed");if(task.duration!==DURATION)issues.push("duration must remain 8000ms");const target=TARGETS.find(value=>task.prompt===`${value}${PROMPT_SUFFIX}`);if(!target)issues.push("prompt must name one published target exactly");const tokens=typeof task.expression==="string"?task.expression.split(" "):[];if(tokens.length!==12||tokens.join(" ")!==task.expression)issues.push("expression must retain exactly 12 single-spaced tokens");if(tokens.some(value=>!SHAPES.includes(value)))issues.push("expression contains a non-published shape");const count=target?tokens.filter(value=>value===target).length:0;if(target&&tokens.some(value=>value!==target&&!SHAPES.filter(item=>item!==target).includes(value)))issues.push("non-target tokens changed");if(target&&count<2)issues.push("expression must retain the minimum-two correction");if(task.answer!==String(count))issues.push("answer must equal the derived target count");const expected=optionSet(count);if(!permutation(task.options,expected))issues.push("options must be the exact published count-option permutation");return issues;
}
const CSS=`
.asc-stage{box-sizing:border-box;width:100%;max-width:430px;min-height:clamp(33rem,calc(100svh - 11rem),45rem);margin-inline:auto;color:#352c3c;outline:none;font-family:"Hiragino Kaku Gothic ProN","Yu Gothic",system-ui,sans-serif}.asc-stage *{box-sizing:border-box}.asc-stage:focus-visible{outline:none}.asc-stage:focus-visible .asc-status{box-shadow:inset 0 0 0 2px #7b568b}.asc-console{position:relative;min-height:inherit;display:grid;grid-template-rows:auto auto auto auto 1fr;gap:.72rem;padding:.9rem;border:1px solid #c8b5d0;border-radius:1.35rem;background:radial-gradient(circle at 50% 0,rgba(255,255,255,.85),transparent 27%),linear-gradient(155deg,#f8f3fa,#e8deed 60%,#d8c9df);box-shadow:inset 0 1px rgba(255,255,255,.9),inset 0 -1.2rem 2.4rem rgba(81,55,92,.08),0 .8rem 1.65rem rgba(61,42,70,.18);overflow:hidden}.asc-console::before{content:"";position:absolute;inset:.38rem;border:1px solid rgba(112,79,125,.14);border-radius:1.08rem;pointer-events:none}.asc-head{position:relative;z-index:1;display:grid;grid-template-columns:auto 1fr auto;align-items:center;gap:.52rem}.asc-brand{color:#5a3b67;font-size:.62rem;font-weight:950;letter-spacing:.15em;white-space:nowrap}.asc-rule{height:2px;background:linear-gradient(90deg,transparent,#b08c69 18%,#d7b878 50%,#b08c69 82%,transparent)}.asc-badge{padding:.24rem .45rem;border:1px solid #b9995f;border-radius:999px;background:#fff9e7;color:#735b2f;font-size:.59rem;font-weight:950;white-space:nowrap}.asc-rail{position:relative;z-index:1;display:grid;grid-template-columns:repeat(12,minmax(0,1fr));gap:.16rem;margin:0;padding:.58rem .45rem;border:1px solid #72577d;border-radius:1rem;background:linear-gradient(155deg,#37283f,#201a27);box-shadow:inset 0 .28rem .75rem rgba(10,6,14,.42),0 .45rem .9rem rgba(44,28,51,.2)}.asc-token{position:relative;display:grid;place-items:center;min-width:0;aspect-ratio:.82;border:1px solid rgba(233,220,237,.52);border-radius:.4rem;background:radial-gradient(circle at 35% 22%,#fff 0 7%,#f5eef7 10% 50%,#d8cadf 77%,#a88faf);color:#4c3457;font:950 clamp(.92rem,4.7vw,1.22rem)/1 system-ui,sans-serif;text-shadow:0 1px #fff;box-shadow:inset .08rem .08rem .15rem rgba(255,255,255,.88),inset -.1rem -.12rem .18rem rgba(56,37,64,.18),0 .16rem .25rem rgba(7,4,9,.34);user-select:none}.asc-status{position:relative;z-index:1;min-height:2.45rem;margin:0;padding:.48rem .6rem;border:1px solid #d4c5da;border-radius:.72rem;background:rgba(255,255,255,.86);color:#63566a;text-align:center;font-size:.74rem;font-weight:900;line-height:1.4}.asc-choices{position:relative;z-index:1;display:grid;grid-template-columns:1fr 1fr;gap:.55rem}.asc-choice{position:relative;min-height:4.25rem;border:1.5px solid #cdbbd5;border-radius:.92rem;background:radial-gradient(circle at 32% 18%,#fff 0 8%,#faf6fb 10% 54%,#e8dde9 78%,#c9b8cf);color:#51365d;font:950 clamp(1.38rem,7vw,1.8rem)/1 system-ui;box-shadow:inset .14rem .14rem .26rem rgba(255,255,255,.86),inset -.15rem -.18rem .27rem rgba(60,39,69,.13),0 .3rem .56rem rgba(31,19,36,.17);touch-action:manipulation;cursor:pointer}.asc-choice::after{content:"";position:absolute;inset:.28rem;border:1px solid rgba(91,61,102,.11);border-radius:.65rem}.asc-choice:focus-visible,.asc-choice.qa-focus{outline:3px dashed #a1792e;outline-offset:3px}.asc-choice.selected{transform:translateY(2px) scale(.985);box-shadow:inset 0 .25rem .42rem rgba(54,34,62,.2),0 .12rem .24rem rgba(24,14,28,.16)}.asc-choice.correct{border-color:#54886b;background:radial-gradient(circle at 32% 18%,#f5fff8,#cde9d8 58%,#89b89c);color:#285940}.asc-choice.wrong{border-color:#a94d5b;background:radial-gradient(circle at 32% 18%,#fff6f6,#efcbd0 58%,#cf8791);color:#762f3c}.asc-choice:disabled{opacity:1;cursor:default}.asc-stage.success .asc-choice:not(.selected),.asc-stage.wrong .asc-choice:not(.selected){filter:saturate(.42);opacity:.68}.asc-stage.timeout .asc-choice{filter:saturate(.35);opacity:.62}.asc-guide{position:relative;z-index:1;align-self:stretch;align-content:center;display:grid;gap:.52rem;min-height:5rem;padding:.7rem;border:1px solid rgba(115,83,128,.18);border-radius:.82rem;background:repeating-linear-gradient(90deg,transparent 0 31px,rgba(113,79,126,.035) 32px 33px),linear-gradient(145deg,rgba(255,255,255,.56),rgba(234,224,239,.62));text-align:center}.asc-guide strong{color:#70557b;font-size:.61rem;letter-spacing:.14em}.asc-guide span{color:#7b6c81;font-size:.61rem;font-weight:800}.asc-stage.success .asc-status{border-color:#82b994;background:#e4f4e8;color:#2a6240}.asc-stage.wrong .asc-status{border-color:#ce878c;background:#fbe8e7;color:#7c3941}.asc-stage.timeout .asc-console{filter:saturate(.58)}.asc-stage.timeout .asc-status{border-color:#a58ba9;background:#f0e7f1;color:#604762}.asc-stage[data-reduced=true] *{animation:none!important;transition:none!important}@media(prefers-reduced-motion:reduce){.asc-stage *{animation:none!important;transition:none!important}}
`;
function render(task,context){
 const issues=validate(task);if(issues.length)throw new Error(`${metadata.id}: ${issues.join("; ")}`);const doc=context.host?.ownerDocument;if(!doc?.createElement)throw new TypeError(`${metadata.id}: context.host.ownerDocument is required`);const make=(tag,className,text)=>{const node=doc.createElement(tag);if(className)node.className=className;if(text!==undefined)node.textContent=text;return node},style=make("style"),stage=make("section","asc-stage ready"),consoleNode=make("div","asc-console"),head=make("header","asc-head"),brand=make("strong","asc-brand","COUNTING TRAY"),rule=make("span","asc-rule"),badge=make("span","asc-badge","12 MARKS"),rail=make("div","asc-rail"),status=make("p","asc-status","12個を左から順に数えてください"),choices=make("div","asc-choices"),guide=make("footer","asc-guide"),guideTitle=make("strong","","OBSERVE · COUNT · CHOOSE"),guideCopy=make("span","","順序を変えず、ひとつずつ確認します");style.textContent=CSS;stage.dataset.reduced=String(Boolean(context.reducedMotion));stage.tabIndex=0;stage.setAttribute("role","group");stage.setAttribute("aria-label","12個の形を数えて4つから答える問題");head.append(brand,rule,badge);rail.setAttribute("role","img");rail.setAttribute("aria-label",task.expression);const tokens=task.expression.split(" ");tokens.forEach((value,index)=>{const token=make("span","asc-token",value);token.setAttribute("aria-hidden","true");token.dataset.position=String(index+1);rail.append(token)});status.setAttribute("role","status");status.setAttribute("aria-live","polite");choices.setAttribute("role","group");choices.setAttribute("aria-label","答えの候補");guide.append(guideTitle,guideCopy);consoleNode.append(head,rail,status,choices,guide);stage.append(consoleNode);context.host.replaceChildren(style,stage);
 const buttons=[],state={phase:"ready",focusIndex:0,selectedIndex:null,correct:null,done:false,committed:false,disposed:false,finishCalls:0,feedbackStages:0,result:null};let staticScene=false;
 const refresh=()=>{stage.className=`asc-stage ${state.phase}`;buttons.forEach((button,index)=>{button.disabled=state.done;button.tabIndex=index===state.focusIndex?0:-1;button.classList.toggle("selected",state.selectedIndex===index);button.classList.toggle("correct",state.selectedIndex===index&&state.correct===true);button.classList.toggle("wrong",state.selectedIndex===index&&state.correct===false)})};
 const commit=payload=>{if(state.committed||state.disposed)return false;state.committed=true;state.finishCalls++;return context.finish(Boolean(state.correct),payload)};
 const choose=index=>{if(state.done||state.disposed||!Number.isSafeInteger(index)||index<0||index>=buttons.length)return false;state.done=true;state.selectedIndex=index;state.focusIndex=index;state.correct=task.options[index]===task.answer;state.phase=state.correct?"success":"wrong";state.result={correct:state.correct,payload:{answerLabel:task.answer}};status.textContent=state.correct?`正解 · ${task.answer}`:`残念 · 正解は「${task.answer}」`;refresh();context.later(()=>{if(state.disposed||state.committed)return;state.feedbackStages++;commit({answerLabel:task.answer})},FEEDBACK_MS);return true};
 const timeout=()=>{if(state.done||state.disposed)return false;state.done=true;state.correct=false;state.phase="timeout";state.result={correct:false,payload:{detail:TIMEOUT_DETAIL,answerLabel:task.answer}};status.textContent=TIMEOUT_DETAIL;refresh();state.feedbackStages++;return commit({detail:TIMEOUT_DETAIL,answerLabel:task.answer})};
 const move=(dx,dy)=>{if(state.done||state.disposed)return false;const row=Math.floor(state.focusIndex/2),column=state.focusIndex%2,nextRow=Math.max(0,Math.min(1,row+dy)),nextColumn=Math.max(0,Math.min(1,column+dx));state.focusIndex=nextRow*2+nextColumn;buttons[state.focusIndex]?.focus({preventScroll:true});refresh();return true};
 task.options.forEach((value,index)=>{const button=make("button","asc-choice",value);button.type="button";button.setAttribute("aria-label",`候補${index+1} ${value}`);context.listen(button,"pointerdown",event=>{event.preventDefault();choose(index)});context.listen(button,"click",event=>{if(event.detail===0){event.preventDefault();choose(index)}});context.listen(button,"focus",()=>{if(!state.done){state.focusIndex=index;refresh()}});buttons.push(button);choices.append(button)});
 context.listen(stage,"keydown",event=>{if(event.key==="ArrowLeft"||event.key==="ArrowRight"||event.key==="ArrowUp"||event.key==="ArrowDown"){event.preventDefault();move(event.key==="ArrowLeft"?-1:event.key==="ArrowRight"?1:0,event.key==="ArrowUp"?-1:event.key==="ArrowDown"?1:0)}else if(event.key==="Enter"||event.key===" "){event.preventDefault();choose(state.focusIndex)}});context.setDeadline(task.duration,()=>{if(!staticScene)timeout()});stage.focus({preventScroll:true});refresh();
 const resetScene=()=>{Object.assign(state,{phase:"ready",focusIndex:0,selectedIndex:null,correct:null,done:false,committed:false,feedbackStages:0,result:null});status.textContent="12個を左から順に数えてください";buttons.forEach(button=>button.classList.remove("qa-focus"))};
 const showScene=name=>{if(state.disposed)return false;staticScene=true;resetScene();if(name==="initial")stage.focus({preventScroll:true});else if(name==="focus"){state.phase="focus";state.focusIndex=(task.options.indexOf(task.answer)+1)%4;buttons[state.focusIndex].focus({preventScroll:true});buttons[state.focusIndex].classList.add("qa-focus");status.textContent=`候補${state.focusIndex+1}を選択中`}else if(name==="wrong"||name==="success"){state.phase=name;state.done=true;state.correct=name==="success";state.selectedIndex=name==="success"?task.options.indexOf(task.answer):(task.options.indexOf(task.answer)+1)%4;state.focusIndex=state.selectedIndex;state.feedbackStages=1;state.result={correct:state.correct,payload:{answerLabel:task.answer}};status.textContent=state.correct?`正解 · ${task.answer}`:`残念 · 正解は「${task.answer}」`}else if(name==="timeout"){state.phase="timeout";state.done=true;state.correct=false;state.feedbackStages=1;state.result={correct:false,payload:{detail:TIMEOUT_DETAIL,answerLabel:task.answer}};status.textContent=TIMEOUT_DETAIL}else return false;refresh();return true};
 const inspect=()=>({phase:state.phase,reduced:Boolean(context.reducedMotion),focusIndex:state.focusIndex,selectedIndex:state.selectedIndex,selectedValue:state.selectedIndex===null?null:task.options[state.selectedIndex],correct:state.correct,done:state.done,committed:state.committed,disposed:state.disposed,finishCalls:state.finishCalls,feedbackStages:state.feedbackStages,frames:0,result:state.result?structuredClone(state.result):null,target:task.prompt.slice(0,1),expression:task.expression,tokens:[...tokens],tokenText:[...rail.children].map(node=>node.textContent),tokenPositions:[...rail.children].map(node=>node.dataset.position),expressionAria:rail.getAttribute("aria-label"),optionText:buttons.map(button=>button.textContent),optionAria:buttons.map(button=>button.getAttribute("aria-label")),disabled:buttons.filter(button=>button.disabled).length,focused:buttons.indexOf(doc.activeElement),classes:buttons.map(button=>button.className),status:status.textContent,dpr:Math.min(3,Math.max(1,Number(context.viewport?.dpr||doc.defaultView?.devicePixelRatio||1))),taskJSON:JSON.stringify(task)});
 const qaApi={choose,move,timeout,showScene,inspect,elements:{stage,rail,tokens:[...rail.children],choices,buttons,status}};if(context.qa&&typeof context.qa==="object")context.qa[metadata.id]=qaApi;context.listen(context.signal,"abort",()=>{state.disposed=true;state.done=true;if(context.qa?.[metadata.id]===qaApi)delete context.qa[metadata.id]},{once:true});
}
export default Object.freeze({metadata,generate,validate,render});

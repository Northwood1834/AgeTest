const PROMPT="ひとつだけ違うものをタップ",HELP="老眼鏡の持ち込み可。",DURATION=7000,SIZE=25,COLUMNS=5;
const PAIRS=Object.freeze([Object.freeze(["シ","ツ"]),Object.freeze(["ぬ","め"]),Object.freeze(["8","B"]),Object.freeze(["老","考"])]),metadata=Object.freeze({id:"attention-odd-v1",introducedIn:"1.0",tier:1,flavor:"satisfying",step:1,family:"attention-odd",category:"attention"});
const exactKeys=(value,keys)=>value&&typeof value==="object"&&!Array.isArray(value)&&Object.keys(value).length===keys.length&&keys.every(key=>Object.hasOwn(value,key)),pairIndex=(normal,odd)=>PAIRS.findIndex(pair=>pair[0]===normal&&pair[1]===odd);
function generate({pick,randomInt}={}){if(typeof pick!=="function")throw new TypeError(`${metadata.id}: pick is required`);if(typeof randomInt!=="function")throw new TypeError(`${metadata.id}: randomInt is required`);const pair=pick(PAIRS);if(!Array.isArray(pair)||pairIndex(pair[0],pair[1])<0)throw new TypeError(`${metadata.id}: pick must return a published pair`);const oddIndex=randomInt(0,24);if(!Number.isInteger(oddIndex)||oddIndex<0||oddIndex>24)throw new RangeError(`${metadata.id}: randomInt must return an integer from 0 to 24`);return{kind:"oddGrid",prompt:PROMPT,help:HELP,normal:pair[0],odd:pair[1],oddIndex,duration:DURATION}}
function validate(task){const issues=[];if(!task||typeof task!=="object"||Array.isArray(task))return["task must be a plain oddGrid object"];if(!exactKeys(task,["kind","prompt","help","normal","odd","oddIndex","duration"]))issues.push("task must retain the exact published seven-key shape");if(task.kind!=="oddGrid")issues.push("kind must remain oddGrid");if(task.prompt!==PROMPT)issues.push("prompt changed");if(task.help!==HELP)issues.push("help changed");if(pairIndex(task.normal,task.odd)<0)issues.push("normal/odd must remain one published ordered pair");if(!Number.isInteger(task.oddIndex)||task.oddIndex<0||task.oddIndex>=SIZE)issues.push("oddIndex must be an integer from 0 to 24");if(task.duration!==DURATION)issues.push("duration must remain 7000ms");return issues}
const CSS=`
.aov-stage{box-sizing:border-box;width:100%;max-width:430px;min-height:clamp(34rem,calc(100svh - 11.5rem),46rem);margin-inline:auto;display:grid;grid-template-rows:auto auto auto auto 1fr;gap:.5rem;color:#2f2a35;outline:none;contain:layout paint;font-family:"Noto Sans JP","Yu Gothic","Hiragino Sans",Arial,sans-serif}.aov-stage *{box-sizing:border-box}.aov-stage:focus-visible{outline:none}.aov-stage:focus-visible .aov-status{box-shadow:inset 0 0 0 2px #765083}.aov-head{display:grid;grid-template-columns:auto 1fr auto;align-items:center;gap:.48rem;padding:.45rem .58rem;border:1px solid #cec1d3;border-radius:.76rem;background:linear-gradient(140deg,#fffdf8,#eee7f1);box-shadow:0 .28rem .75rem rgba(58,41,65,.1)}.aov-brand{color:#50365a;font-size:.64rem;font-weight:950;letter-spacing:.12em;white-space:nowrap}.aov-rule{height:2px;border-radius:999px;background:linear-gradient(90deg,transparent,#8f7498 17%,#c5a76c 50%,#8f7498 83%,transparent)}.aov-badge{padding:.23rem .46rem;border:1px solid #a48aab;border-radius:999px;background:#392a40;color:#fff;font-size:.61rem;font-weight:950;white-space:nowrap}.aov-board{position:relative;padding:.67rem;border:1px solid #725b7d;border-radius:1.08rem;background:radial-gradient(circle at 50% 0,rgba(255,255,255,.9),rgba(243,238,245,.94) 42%,rgba(216,207,221,.98));box-shadow:0 .75rem 1.55rem rgba(48,34,55,.21),inset 0 0 0 1px rgba(255,255,255,.78);overflow:hidden}.aov-board::before{content:"";position:absolute;inset:0;pointer-events:none;background:repeating-linear-gradient(90deg,transparent 0 27px,rgba(83,62,91,.025) 28px 29px),repeating-linear-gradient(0deg,transparent 0 27px,rgba(83,62,91,.025) 28px 29px)}.aov-grid{position:relative;z-index:1;display:grid;grid-template-columns:repeat(5,minmax(0,1fr));gap:.37rem}.aov-cell{position:relative;display:grid;place-items:center;min-width:0;aspect-ratio:1;border:1px solid #c9becd;border-radius:.66rem;background:linear-gradient(145deg,#fffefd,#f6f1f7 58%,#dfd6e3);box-shadow:0 .23rem .44rem rgba(49,37,54,.14),inset 0 1px rgba(255,255,255,.98);color:#252129;font:900 clamp(1.2rem,6.35vw,1.66rem)/1 "Noto Sans JP","Yu Gothic","Hiragino Sans",Arial,sans-serif;font-style:normal;letter-spacing:0;text-transform:none;writing-mode:horizontal-tb;cursor:pointer;touch-action:manipulation;user-select:none}.aov-cell:focus-visible{z-index:2;outline:3px dashed #6e4f79;outline-offset:2px;box-shadow:0 0 0 6px rgba(110,79,121,.15),0 .28rem .55rem rgba(43,30,49,.2)}.aov-cell:disabled{opacity:1;color:#252129}.aov-cell[data-selected=true]{transform:translateY(1px) scale(.97);border-color:#b18b3e;background:linear-gradient(#fff9dc,#efdea7);box-shadow:inset 0 .2rem .36rem rgba(100,75,24,.14)}.aov-stage[data-phase=CHECK] .aov-cell[data-selected=true]{box-shadow:0 0 0 5px rgba(183,145,65,.21),inset 0 .2rem .36rem rgba(100,75,24,.14)}.aov-stage[data-result=success] .aov-cell[data-selected=true]{border-color:#4b8560;background:linear-gradient(#ebf9ed,#b9ddc2);box-shadow:0 0 0 5px rgba(69,134,88,.2)}.aov-stage[data-result=wrong] .aov-cell[data-selected=true]{border-color:#aa4d58;background:linear-gradient(#fdebea,#efbdbc);box-shadow:0 0 0 5px rgba(170,77,88,.19)}.aov-stage[data-result=timeout] .aov-grid{filter:saturate(.48);opacity:.62}.aov-banner{position:relative;z-index:2;min-height:3.05rem;margin:.58rem 0 0;padding:.62rem;border:2px solid rgba(255,255,255,.8);border-radius:.82rem;color:#fff;display:grid;place-items:center;text-align:center;box-shadow:0 .38rem .82rem rgba(35,24,40,.2);font:950 clamp(.82rem,3.8vw,1rem)/1.25 system-ui;white-space:nowrap}.aov-banner[data-visible=false]{visibility:hidden}.aov-stage[data-result=success] .aov-banner{background:rgba(46,111,66,.96)}.aov-stage[data-result=wrong] .aov-banner{background:rgba(143,54,65,.96)}.aov-stage[data-result=timeout] .aov-banner{background:rgba(77,58,82,.96)}.aov-status{min-height:2.65rem;margin:0;padding:.49rem .62rem;border:1px solid #d5cbd9;border-radius:.72rem;background:#fff;color:#5d5362;text-align:center;font:850 .74rem/1.4 system-ui}.aov-stage[data-phase=CONTACT] .aov-status,.aov-stage[data-phase=CHECK] .aov-status{border-color:#d6bd79;background:#fff7dc;color:#6d531b}.aov-stage[data-result=success] .aov-status{border-color:#83b991;background:#e5f4e8;color:#2b623d}.aov-stage[data-result=wrong] .aov-status{border-color:#cd878a;background:#fbe8e6;color:#793a40}.aov-stage[data-result=timeout] .aov-status{border-color:#a28aa5;background:#f0e8f0;color:#5d485f}.aov-hint{margin:0;color:#746a78;text-align:center;font:750 .62rem/1.35 system-ui}.aov-guide{align-self:stretch;align-content:center;display:grid;gap:.56rem;min-height:5.5rem;padding:.72rem;border:1px solid #d9cedd;border-radius:.8rem;background:radial-gradient(circle at 15% 25%,rgba(113,80,126,.1) 0 3px,transparent 4px),radial-gradient(circle at 85% 75%,rgba(113,80,126,.08) 0 3px,transparent 4px),repeating-linear-gradient(0deg,transparent 0 24px,rgba(91,67,101,.035) 25px 26px),linear-gradient(145deg,rgba(255,255,255,.8),rgba(235,227,239,.82));text-align:center}.aov-guide strong{color:#654c70;font:950 .62rem/1.2 system-ui;letter-spacing:.13em}.aov-guide span{color:#7a6f7e;font:750 .6rem/1.35 system-ui}.aov-stage[data-reduced=true] *{animation:none!important;transition:none!important}@media(prefers-reduced-motion:reduce){.aov-stage *{animation:none!important;transition:none!important}}
`;
function render(task,context){
 const issues=validate(task);if(issues.length)throw new Error(`${metadata.id}: ${issues.join("; ")}`);const doc=context.host?.ownerDocument;if(!doc?.createElement)throw new TypeError(`${metadata.id}: context.host.ownerDocument is required`);const view=doc.defaultView||globalThis,reduced=Boolean(context.reducedMotion),make=(tag,className,text)=>{const node=doc.createElement(tag);if(className)node.className=className;if(text!==undefined)node.textContent=text;return node},style=make("style"),stage=make("section","aov-stage"),head=make("header","aov-head"),brand=make("strong","aov-brand","GLYPH INSPECTION"),rule=make("span","aov-rule"),badge=make("span","aov-badge","5 × 5"),board=make("div","aov-board"),grid=make("div","aov-grid"),banner=make("div","aov-banner"),status=make("p","aov-status","25文字を順に見て、違うものをひとつ選びます"),hint=make("p","aov-hint","セルをタップ / 矢印キーで5×5移動 / Enter・Space"),guide=make("footer","aov-guide"),guideTitle=make("strong","","POSITION 01 — 25"),guideCopy=make("span","","形・向き・線の間隔を同じ順序で見比べます");style.textContent=CSS;stage.dataset.phase="INITIAL";stage.dataset.result="";stage.dataset.reduced=String(reduced);stage.tabIndex=0;stage.setAttribute("role","group");stage.setAttribute("aria-label","5行5列、25文字の中から違うものをひとつ探します");head.append(brand,rule,badge);grid.setAttribute("role","group");grid.setAttribute("aria-label","5行5列の文字探索グリッド");banner.dataset.visible="false";banner.setAttribute("role","status");board.append(grid,banner);status.setAttribute("role","status");status.setAttribute("aria-live","polite");guide.append(guideTitle,guideCopy);stage.append(head,board,status,hint,guide);context.host.replaceChildren(style,stage);
 const cells=[],state={phase:"INITIAL",focusIndex:0,selectedIndex:null,busy:false,done:false,disposed:false,result:null,detail:"",correct:null,stages:0,finishCalls:0};let token=0,staticScene=false;const glyphAt=index=>index===task.oddIndex?task.odd:task.normal;
 const update=()=>{stage.dataset.phase=state.phase;stage.dataset.result=state.result||"";stage.dataset.busy=String(state.busy);banner.dataset.visible=String(Boolean(state.result));banner.textContent=state.detail;cells.forEach((cell,index)=>{cell.tabIndex=index===state.focusIndex?0:-1;cell.disabled=state.busy||state.done;cell.dataset.selected=String(index===state.selectedIndex);cell.setAttribute("aria-pressed",String(index===state.selectedIndex))})};
 const finish=(correct,detail,result)=>{state.finishCalls++;if(state.done||state.disposed)return false;state.done=true;state.busy=false;state.correct=correct;state.result=result;state.detail=detail;state.phase=result.toUpperCase();status.textContent=detail;update();return context.finish(correct,{detail})};
 const settle=()=>{if(state.done||state.disposed||state.selectedIndex===null)return false;const correct=state.selectedIndex===task.oddIndex;return finish(correct,correct?"見つかってしまいました。":"そっくりさんでした。",correct?"success":"wrong")};
 const choose=index=>{if(state.done||state.disposed||state.busy||!Number.isInteger(index)||index<0||index>=SIZE)return false;const active=++token;state.selectedIndex=index;state.focusIndex=index;state.busy=true;state.phase="CONTACT";status.textContent=`${index+1}番目を確認しています`;update();if(reduced){context.later(()=>{if(active===token&&!state.done&&!state.disposed){state.stages++;state.phase="CHECK";status.textContent=`${index+1}番目を照合しました`;update()}},120);context.later(()=>{if(active===token&&!state.done&&!state.disposed){state.stages++;settle()}},260)}else context.later(()=>{if(active===token&&!state.done&&!state.disposed){state.stages++;settle()}},160);return true};
 const move=(dx,dy)=>{if(state.done||state.disposed||state.busy)return false;const row=Math.floor(state.focusIndex/COLUMNS),column=state.focusIndex%COLUMNS,nextRow=Math.max(0,Math.min(4,row+dy)),nextColumn=Math.max(0,Math.min(4,column+dx));state.focusIndex=nextRow*COLUMNS+nextColumn;cells[state.focusIndex]?.focus({preventScroll:true});status.textContent=`${state.focusIndex+1}番目に移動`;update();return true};
 for(let index=0;index<SIZE;index++){const cell=make("button","aov-cell",glyphAt(index));cell.type="button";cell.setAttribute("aria-label",`${index+1}番目 ${cell.textContent}`);context.listen(cell,"pointerdown",event=>{event.preventDefault();choose(index)});context.listen(cell,"click",event=>{if(event.detail===0){event.preventDefault();choose(index)}});context.listen(cell,"focus",()=>{if(!state.busy&&!state.done){state.focusIndex=index;update()}});cells.push(cell);grid.append(cell)}
 context.listen(stage,"keydown",event=>{if(event.key==="ArrowLeft"||event.key==="ArrowRight"||event.key==="ArrowUp"||event.key==="ArrowDown"){event.preventDefault();move(event.key==="ArrowLeft"?-1:event.key==="ArrowRight"?1:0,event.key==="ArrowUp"?-1:event.key==="ArrowDown"?1:0)}else if(event.key==="Enter"||event.key===" "){event.preventDefault();choose(state.focusIndex)}});if(view&&typeof view.dispatchEvent==="function")context.listen(view,"resize",update,{passive:true});
 const expire=()=>{token++;return finish(false,"違うものは群衆に紛れました。","timeout")};context.setDeadline(task.duration,()=>{if(!staticScene)expire()});
 const resetScene=()=>{token++;Object.assign(state,{phase:"INITIAL",focusIndex:0,selectedIndex:null,busy:false,done:false,result:null,detail:"",correct:null,stages:0});status.textContent="25文字を順に見て、違うものをひとつ選びます";cells.forEach(cell=>cell.classList.remove("qa-focus"))},normalIndex=()=>task.oddIndex===24?23:task.oddIndex+1;
 const showScene=name=>{if(state.disposed)return false;staticScene=true;resetScene();if(name==="initial")stage.focus({preventScroll:true});else if(name==="focus"){state.phase="FOCUS";state.focusIndex=normalIndex();cells[state.focusIndex].focus({preventScroll:true});cells[state.focusIndex].classList.add("qa-focus");status.textContent=`${state.focusIndex+1}番目にキーボードフォーカス`}else if(name==="wrong"){state.phase="WRONG";state.focusIndex=normalIndex();state.selectedIndex=state.focusIndex;state.done=true;state.correct=false;state.result="wrong";state.detail="そっくりさんでした。";status.textContent=state.detail}else if(name==="success"){state.phase="SUCCESS";state.focusIndex=task.oddIndex;state.selectedIndex=task.oddIndex;state.done=true;state.correct=true;state.result="success";state.detail="見つかってしまいました。";status.textContent=state.detail}else if(name==="timeout"){state.phase="TIMEOUT";state.done=true;state.correct=false;state.result="timeout";state.detail="違うものは群衆に紛れました。";status.textContent=state.detail}else return false;update();return true};
 const inspect=()=>({phase:state.phase,focusIndex:state.focusIndex,selectedIndex:state.selectedIndex,busy:state.busy,done:state.done,disposed:state.disposed,result:state.result,detail:state.detail,correct:state.correct,stages:state.stages,frames:0,finishCalls:state.finishCalls,pairIndex:pairIndex(task.normal,task.odd),oddIndex:task.oddIndex,cellCount:cells.length,oddCount:cells.filter(cell=>cell.textContent===task.odd).length,glyphs:cells.map(cell=>cell.textContent),arias:cells.map(cell=>cell.getAttribute("aria-label")),classes:cells.map(cell=>cell.className),dpr:Math.min(3,Math.max(1,Number(context.viewport?.dpr||view.devicePixelRatio||1))),status:status.textContent,reduced,taskJSON:JSON.stringify(task)}),qaApi={choose,move,expire,showScene,inspect,elements:{stage,board,grid,banner,cells,status}};update();stage.focus({preventScroll:true});if(context.qa&&typeof context.qa==="object")context.qa[metadata.id]=qaApi;context.listen(context.signal,"abort",()=>{state.disposed=true;state.done=true;state.busy=false;token++;if(context.qa?.[metadata.id]===qaApi)delete context.qa[metadata.id]},{once:true});
}
export default Object.freeze({metadata,generate,validate,render});

# パイロット後の作者レビュー項目

最終更新: 2026-07-25

以下はビルドを止めずに暫定値で実装した項目です。パイロット版では動作しますが、公開後の反応を見てmumupoikatsuが最終判断します。

## 1. 63タイプの構成とTier

- 作者は最大100タイプまでの追加を許可し、50タイプ以上なら合理的な時点で停止可とした。パイロットはQA範囲を過度に広げないため52タイプで停止し、v1.4.0でドット絵バトル、v1.5.0で擬似3Dの自走ステージ、v1.6.0で連鎖パズル、v1.7.0でピン抜きパズル、v1.8.0で広告風ミニゲーム4種、v1.9.0で線つなぎ、v1.10.0で配管つなぎ、v1.11.0でネジ抜きを追加して63タイプとした。
- v1.8問題パックは合計63タイプ、Tier 1 / 2 / 3 = 18 / 30 / 15。
- 毎セッションに `classic`（無難）、`satisfying`（解ける快感）、`quirky`（変化球）、`wild`（「これ脳トレ？」）を各1タイプ以上入れる。
- カテゴリは最大2問、未経験・苦手カテゴリを優先し、直近の重複を避ける。
- この配分と各タイプのTierは実装上の暫定採用。プレイ完了率と感想を見て変更する場合は、既存IDの意味を変えず新IDまたは明示的なTier変更として扱う。

## 2. 乙女ゲーム風の会話問題

- 最初の50タイプ到達時、未公開だった経路計画問題案を会話問題 `social-date-v1` に置き換えた。
- Tier 2・会話力。架空の成人男性3人から1人が選ばれ、文脈を受けた選択を3回成功させるとデート成立。
- 画像はPhotobook/Terra経由のKrea2 HD生成物。今後画像が必要な場合もPhotobookのTerraへ依頼する。
- 正解は社交能力の診断ではなく、このシナリオ内の読解ゲームとしてのみ扱う。
- 派生として、既存画像を使い回さない `social-partner-mood-v1` を追加。不機嫌なパートナーへ謝罪・受容・共同提案をつなぐ3段階で、威圧や支配を正解にしない。
- `language-english-v1` は問題文・補助文・選択肢をすべて英語で表示する。

## 3. ドット絵コマンド戦闘（v1.4.0）

- `calculation-rpg-battle-v1`。Tier 2・計算力・`wild`。制限時間は長め（標準90秒、ゆったり135秒）。
- モンスターは24×24ドットの2フレームスプライトをアプリ内で描画する。既存作品の画像・音楽・固有UIは使わず、造形・配色・名前はすべてこのアプリ用に描き起こした。呪文名も一般語（かえん・ひょうけつ・かまいたち・いやしの光・ねむりのうた）に置き換えている。
- 生成のたびに、種族2〜3体・HP・攻撃力・弱点属性・色相・行動プールが変わる。3体編成のときはHPと攻撃力を下げて釣り合いを取る。
- 敵の行動は毎ターン抽選で、こうげき・呪文・いやし・ねむり・ちからため・みをまもる・なかまを呼ぶ・にげるから選ぶ。ドラクエ6のように攻撃時は前へ踏み込み、被弾時は白く点滅して揺れる。
- コマンドは たたかう・まほう・とくぎ・どうぐ・なかま・にげる。「にげる」は押した時点で不正解にする（作者指定）。
- 主人公名はプレイヤーのプロフィール名をそのまま使う。「なかま」はムームー・ポイント・日本円・メガネ・スマイル・パンダ・イヌ・ネコから毎回2人が選ばれ、各1回だけ行動する。
- 敵を全滅させると正解。HPが尽きる、にげる、時間切れは不正解。

## 4. 擬似3D自走ステージ（v1.5.0）

- `spatial-lane-run-v1`。Tier 2・空間認識・`wild`。制限時間45秒（ゆったり67.5秒、走行速度も2割落とす）。
- WebGLを使わず、Canvas 2Dの遠近投影で描く。解像度は端末のdevicePixelRatioに合わせ、道路・空・稜線・並木・障害物・主人公はすべてグラデーションと陰影で描画する（レトロ縮小はしない）。ビルドなし・外部依存なしの方針は維持。
- 自走式で、プレイヤーの操作は左右の位置とジャンプのタイミングだけ。岩の壁はよけ、穴の溝は跳び、木箱は跳んでこわす（ボーナス）。赤いTNTは木箱と同じ箱の形だが、跳んでも当たっても失敗するため、レーン変更だけが正解になる（跳ぶ反射を止める抑制の要素）。入力は `pointerdown` で受け、踏み切りから障害物を越える高さまで75ms、滞空806ms、押しどきの猶予は約0.8秒。
- 作者マスコットがお邪魔キャラとしてレーンを横切る。ぶつかると失敗、跳び越せば通過できる。
- ステージ構成・テーマ（朝焼け／昼／夕暮れ）・作者の出現位置は毎回生成する。生成500回で通過不能な配置は0件、レイアウト重複も0件。ゴール到達で正解、接触・落下・時間切れは不正解。
- ジャンプ抜きでは越えられない配置を必ず含める（自動テストでよけるだけの攻略が失敗することを確認済み）。

## 5. 連鎖パズル（v1.6.0）

- `prediction-chain-puzzle-v1`。Tier 2・未来予知・`satisfying`。制限時間60秒、持ち手は3手。
- 7列×9段の落ちものパズル。同じ顔が縦横に4つつながると消え、上のブロックが落ちて連鎖する。
- 盤面はランダム生成では連鎖がほぼ成立しない（ランダム300回で3連鎖0件）ため、3連鎖の手順から逆算して積み方を構築し、全列の落下を総当たりで検証してから出題する。生成300回で3連鎖以上100%、最遅26ms。
- 持ち手は1手。盤面には「4つ消えるが上に何も乗っておらず連鎖しない」ひっかけを必ず2か所以上置き、連鎖する列を読ませる。でたらめに落とした場合の正解率は実測15.8%（4択問題の25%より低い）。連鎖の仕込み位置はランダムで、解の列は7列中6列に分散する。
- キャラクターはイヌ・ネコ・パンダ・😀・🤓・🤑の6種から毎回4種を抽選。連鎖ごとに連鎖数バナー、粒子、リング、画面揺れが強くなる。
- 描画はCanvas 2Dで端末解像度に合わせる。ドット絵ではなく、グラデーションと光沢のポップな見た目にした。

## 6. ピン抜きパズル（v1.7.0）

- `prediction-pin-pull-v1`。Tier 2・未来予知・`wild`。制限時間50秒。広告でよく見る「ピンを抜くと溶岩が流れる」形式を、独自の絵と独自のルールで作った（既存作品の名称・画像・UIは使わない）。
- ルールは4つ。溶岩が主人公にかかれば失敗、水と溶岩が混ざると石になって道をふさぐ、コインは主人公に届けば回収、ピンを抜いた部屋は空きパイプになり後から来たものが素通りする。
- レイアウト3種（合流・段差・縦3段）と中身の組み合わせで毎回変化。モンスターはコインを食べる罠。
- 生成時に6通りの順番をすべて総当たりし、「正解が1通りだけ」かつ「失敗する順番が2通り以上」の盤面だけを採用する。生成400回で条件を満たさなかったものは0件、最遅1ms。
- でたらめに選んだ場合の正解率は1/6＝約17%。

## 7. 広告風ミニゲーム4種（v1.8.0）

- いずれも独自の絵とルールで作り、既存作品の名称・画像・UIは使わない。出題前に探索で解けることを保証する方針は共通。
- `attention-water-sort-v1`（注意力・Tier 2・75秒）。5本のボトル、同じ色の上にだけ注げる。幅優先探索で最短4〜7手の配置だけを採用し、手数制限は最短+3手。
- `calculation-gate-run-v1`（計算力・Tier 2・35秒）。上下の門を3回選び、最後の敵より多い数で勝つ。8通りの経路を全列挙し、勝てる道が1〜2通りだけの配置を採用する。開始直後は文字を出さず、ただ歩く時間を2.9秒置いて状況を読ませる（作者指定でカウントダウン表示と敵数表示は入れない）。
- `spatial-park-jam-v1`（空間認識・Tier 3・75秒）。5×5の駐車場から赤い車を出す。幅優先探索で最短4〜8手の配置だけを採用し、手数制限は最短+3手。
- `spatial-rope-untangle-v1`（空間認識・Tier 2・70秒）。杭をドラッグして線分交差をゼロにする。初期交差3〜7本の配置だけを採用し、交差判定は線分交差の厳密計算で行う。

## 8. 線つなぎ（v1.9.0）

- `spatial-flow-link-v1`（空間認識・Tier 3・80秒）。5×5で同じ色の点を線でつなぎ、全25マスを埋めると正解。
- 盤面は全マスを通る一筆書きを作ってから4〜5本に切り分ける逆算方式で、解の存在を保証する。一筆書きの生成は当初のバックトラッキングで最遅3秒かかったため、バックバイト法（1手あたり1回の反転で経路を保つ乱択）に置き換えて最遅5msにした。
- 指を速く動かすと `pointermove` がマスを飛ばすため、飛んだ区間は1マスずつ補間して伸ばす。
- つながり判定は両端が対になっているかで見る。片方向だけを見ていた実装では、2つ目の点から引いた線が未接続のままになる不具合があった（作者の指摘で修正）。

## 9. 配管つなぎ（v1.10.0）

- `spatial-pipe-flow-v1`（空間認識・Tier 2・70秒）。4×4のパイプをタップで90度ずつ回し、左の水源から右のゴールへ通す。
- 通っている配置を先に作ってから各ピースをランダムに回す逆算方式で、回転だけで必ず解ける。開始時点で開通している盤面は捨てる。
- 水が届いているパイプは青、届いていないパイプは灰色で描き、どこまで通ったかを見せる。

## 10. ネジ抜き（v1.11.0）

- `attention-screw-out-v1`（注意力・Tier 2・60秒）。3×3の穴に縦横の板を架け、上の板の下に潜ったボルトは回せない。3枚すべて外せば正解、隠れたボルトを押した時点で失敗。
- 720通りの順番をすべてシミュレートし、成功する順番が全体の20%以下の配置だけを採用する。
- 当初は板ごとに高さをずらして描いたため、判定上は下敷きでも絵では重なって見えないボルトがあり、作者から「感覚的に分かりにくい」と指摘を受けた。板を縦横に交差させる形に変更し、隠れたボルトは影として透かして位置を示すようにした。

## 11. 難易度・時間

- 各制限時間、反応ボーナス、タイミング許容幅は臨床値ではなくゲーム用の初期調整。
- 絵文字ランナーは、丸太が遅すぎて難しくなる実プレイ指摘を受け、到達時間を約1.8秒（`travelMs: 2800`）へ短縮し、ジャンプを780msへ調整した。
- v1.2.1で実プレイ指摘を反映し、記憶選択問題の初期提示を1.9秒から3.0秒、記憶経路の回答開始を2.8秒から3.2秒へ延長。乙女ゲーム会話は28秒から18秒、パートナー会話は30秒から20秒へ短縮した。
- 続く実機指摘を受け、v1.2.2で記憶選択問題と記憶経路の初期フェーズをともに5.0秒へ延長。経路の各マスも330msから750msの点灯へ延ばし、約1.3秒間隔で順に提示する。
- v1.2.3では中高年の知覚・反応・視覚ワーキングメモリ・疲労・モバイル可読性の文献照合を反映。5秒の記憶提示、750ms点灯、単純反応1.5秒、12問、10分休憩は維持した。男女別補正は採用しない。
- 同版でプロファイルごとの任意の「ゆったり」を追加。読字・選択・複数操作課題は1.5倍、会話は18/20秒から27/30秒、ランナーは速度を変えず成功判定を広げる。記憶提示・単純反応・時間感覚は変えない。XPとクールダウンは通常どおりだが、標準番付には混ぜない。
- 同版で主要な問題文・回答・会話を16px以上、主要操作を48px前後へ拡大。補助文字、成功・失敗色、主要ボタンのコントラストを通常文字4.5:1以上になる配色へ変更した。
- v1.2.4で絵文字FPSの命中済みボタンをDOMから即時除去し、`disabled` の3Dボタンや消失アニメーション層を残さない方式へ修正した。iPhone Safariでの最終確認は実機で行う。
- 同版で時間感覚課題の開始タップを廃止し、3・2・1の自動カウント後に計測を開始する。正解の手掛かりになっていた3秒周期のオーブ拡縮と進行バーを計測中は表示せず、開始後は静的な色変化だけにした。
- v1.2.5で記憶経路の3×3グリッドを最大16remから18remへ拡大。点灯マスは縮小表示をやめて1.1倍にし、太い輪郭・二重の外光・白い星印で、750msの点灯を色だけに頼らず識別できるようにした。
- v1.2.6で作者中ボスを、命中時だけ移動する方式から問題中ずっと画面内を反射移動する方式へ変更。命中ごとに方向と速度を変える。`prefers-reduced-motion` では連続移動を止め、命中時の位置変更だけを維持する。
- v1.2.7でジャンプ課題に1.5秒の「よーい…」待機を追加。待機が終わるまで丸太・移動・制限時間を開始せず、先行タップも失敗扱いにしない。`prefers-reduced-motion` の静止画モードにも同じ待機時間を適用する。
- v1.2.8で作者中ボスの常時移動を撤回し、命中後だけ別位置へ移るv1.2.5の挙動へ戻した。同版で「逃げる紫」は移動中のタップ開始時点で確保を成立させ、指を離すまでに的が動いて背景クリックと判定される経路を遮断した。
- v1.2.9でゴルフの操作を、ボールからホールへ線を引く方式から「ボールを押さえ、打ちたい方向と反対へパターを引き、離して打つ」方式へ変更。予測線は引いた方向ではなく実際の打球方向を示し、パターも指に合わせて後退してから前方へ振る。
- v1.3.0で作者中ボスを再設計。3回の確保に「行き先を残像で予告する瞬間移動」「不規則なフェイントダンス」「最大約0.43秒だけ画面外へ消えて端から帰還」を重複なし・ランダム順で割り当てる。移動中も接触開始時点で確保し、制限時間を7秒から10秒へ延ばして理不尽さを抑える。`prefers-reduced-motion` では連続ダンスと帰還アニメーションを止め、残像予告と離散的な位置変更だけを残す。
- v1.3.1で「逃げる紫」を小幅な往復アニメーションから、画面幅の40〜50%/秒・高さの36〜46%/秒で枠内を反射する連続移動へ変更。壁に当たるたび速度を再抽選し、接触開始時点の確保判定は維持する。`prefers-reduced-motion` では連続移動を行わない。
- v1.3.2でキューブ回転課題の、回転後の正面色をそのまま見て答えられる構造を廃止。最初の2.4秒だけ正面・右面・上面の色を見せ、全色を「？」で隠してから2〜3回の回転手順を提示する。回答は最終状態を記憶と心的回転で求めるが、初期状態で見えない面が正解になる手順は出さない。
- v1.3.3でアプリ用アイコンを作者マスコットのTerra I2I生成へ変更。姉妹アプリの太い輪郭・単純なシルエット・小サイズ可読性を踏襲し、にっこり笑って一本の杖をつく姿を、初老テストの薄紫背景と紫アクセントで描く。PWA、Apple Touch Icon、ブラウザfaviconで同じ正本を使う。
- v1.1で最大4セットを試した後、作者判断によりv1.2から「前半3セット → 10分休憩 → 後半3セット → 20時間枠終了まで休む」へ変更した。最大時の実プレイは約24〜36分を2ブロックに分割する。これは医学的最適値ではなく製品ルール。
- 12問と格付け境界（処老85以上、初老60以上、中老40以上、大老40未満）は現在の採用仕様。変更はパイロットデータと作者判断を要する。

### 参考にした主要資料

- Der & Deary (2006), *Psychology and Aging*. DOI: `10.1037/0882-7974.21.1.62`
- Woods et al. (2015), *Frontiers in Human Neuroscience*. DOI: `10.3389/fnhum.2015.00131`
- Ko et al. (2014), *Attention, Perception, & Psychophysics*. DOI: `10.3758/s13414-013-0585-z`
- Albulescu et al. (2022), *PLOS ONE*. DOI: `10.1371/journal.pone.0272460`
- Hou et al. (2022), *Frontiers in Psychology*. DOI: `10.3389/fpsyg.2022.931646`
- Gomez-Hernandez et al. (2023), *JMIR mHealth and uHealth*. DOI: `10.2196/43186`
- W3C, [WCAG 2.2](https://www.w3.org/TR/WCAG22/)

## 12. 実機確認

- iPhone Safariの確認は作者と後日実機で行う。今回の完了判定に未実施を隠して含めない。

## 13. 次期候補

- 追加問題のネタが尽きた場合は、PhotobookのTerraに画像を活かした問題案を依頼する。画像生成も同じTerra/Krea2経路を使い、既存画像の無断使い回しはしない。
- オンラインランキングは `RANKING_DESIGN.md` の別モード要件を満たし、作者が明示的に採用した場合のみ。

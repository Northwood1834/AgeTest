import game from"../src/games/attention-odd-v1.js";import{createGameRuntime}from"../src/game-kernel.js";
const PAIRS=Object.freeze([Object.freeze(["シ","ツ"]),Object.freeze(["ぬ","め"]),Object.freeze(["8","B"]),Object.freeze(["老","考"])]),INDICES=Object.freeze([6,18,12,23]),CASES=Object.freeze(PAIRS.map((pair,family)=>{const trace=[],task=game.generate({pick(values){trace.push({kind:"pick",values:values.map(value=>[...value]),value:[...pair]});return values[family]},randomInt(min,max){const value=INDICES[family];trace.push({kind:"randomInt",min,max,value});return value}});return Object.freeze({task:Object.freeze(task),trace:Object.freeze(trace)})}));
const params=new URLSearchParams(location.search),width=Number(params.get("width"))||393,motion=params.get("motion")==="reduced"?"reduced":"normal",family=Math.max(0,Math.min(3,Number(params.get("family"))||0)),requestedScene=params.get("scene")||"initial",reduced=motion==="reduced",live=params.get("live")==="1",auto=params.get("auto")==="1",task=CASES[family].task,host=document.querySelector("#challenge"),result=document.querySelector("#result"),qa={},finishes=[],errors=[];document.body.style.width=`${width}px`;addEventListener("error",event=>errors.push(event.message||"page error"));addEventListener("unhandledrejection",event=>errors.push(String(event.reason)));
const runtime=createGameRuntime({host,qa,reducedMotion:reduced,viewport:{width,height:innerHeight,dpr:devicePixelRatio},onFinish:(correct,payload)=>{finishes.push({correct,payload});result.hidden=false;result.textContent=`${correct?"成功":"失敗"} — ${payload.detail}`}});game.render(structuredClone(task),runtime.context);const api=qa[game.metadata.id];if(!live&&!auto&&!api.showScene(requestedScene))throw new Error(`unknown scene ${requestedScene}`);
const evidence=()=>{const resources=performance.getEntriesByType("resource"),external=resources.filter(entry=>new URL(entry.name).origin!==location.origin),state=api.inspect();return{variant:"module",family,trace:structuredClone(CASES[family].trace),requestedScene,renderedScene:requestedScene,live,auto,motion,width,dpr:devicePixelRatio,task:structuredClone(task),state,finishes:structuredClone(finishes),runtime:runtime.inspect(),layout:{scrollWidth:document.body.scrollWidth,innerWidth},resources:resources.map(entry=>entry.name),external:external.map(entry=>entry.name),errors:[...errors]}};
if(auto){const cells=host.querySelectorAll(".aov-cell"),stage=host.querySelector(".aov-stage");if(family%2===0)cells[task.oddIndex].dispatchEvent(new PointerEvent("pointerdown",{bubbles:true,cancelable:true,pointerId:17,pointerType:"touch"}));else{stage.focus();const row=Math.floor(task.oddIndex/5),column=task.oddIndex%5;for(let i=0;i<column;i++)stage.dispatchEvent(new KeyboardEvent("keydown",{bubbles:true,cancelable:true,key:"ArrowRight"}));for(let i=0;i<row;i++)stage.dispatchEvent(new KeyboardEvent("keydown",{bubbles:true,cancelable:true,key:"ArrowDown"}));stage.dispatchEvent(new KeyboardEvent("keydown",{bubbles:true,cancelable:true,key:"Enter"}))}}
if(params.get("measureLog")==="1"){const deltas=[];let previous=performance.now(),count=0;const measure=now=>{deltas.push(now-previous);previous=now;if(++count<100)requestAnimationFrame(measure);else{const stable=deltas.slice(5),state=api.inspect(),query=new URLSearchParams({frames:String(stable.length),averageFrameMs:(stable.reduce((a,b)=>a+b,0)/stable.length).toFixed(3),maxFrameMs:Math.max(...stable).toFixed(3),gameFrames:String(state.frames),dpr:String(state.dpr),cellCount:String(state.cellCount)});location.replace(`attention-odd-v1.performance-result.html?${query}`)}};requestAnimationFrame(measure)}
window.__ATTENTION_ODD_MODULE_FIXTURE__={game,cases:CASES.map(value=>({task:structuredClone(value.task),trace:structuredClone(value.trace)})),task:structuredClone(task),qa,api,runtime,evidence};document.documentElement.dataset.ready="true";

import test from"node:test";import assert from"node:assert/strict";import{readFile}from"node:fs/promises";import{setTimeout as delay}from"node:timers/promises";import{createGameRuntime}from"../src/game-kernel.js";
const moduleUrl=new URL("../src/games/attention-odd-v1.js",import.meta.url),source=await readFile(moduleUrl,"utf8"),legacySource=await readFile(new URL("../app.js",import.meta.url),"utf8"),game=(await import(`data:text/javascript;base64,${Buffer.from(source).toString("base64")}`)).default,ID="attention-odd-v1",PROMPT="ひとつだけ違うものをタップ",HELP="老眼鏡の持ち込み可。",PAIRS=[["シ","ツ"],["ぬ","め"],["8","B"],["老","考"]];
const task=(pair=0,index=12)=>game.generate({pick:items=>items[pair],randomInt:()=>index});
class FakeClassList{constructor(owner){this.owner=owner;this.values=new Set()}reset(value){this.values=new Set(String(value||"").split(/\s+/).filter(Boolean))}sync(){this.owner._className=[...this.values].join(" ")}add(...values){values.forEach(value=>this.values.add(value));this.sync()}remove(...values){values.forEach(value=>this.values.delete(value));this.sync()}contains(value){return this.values.has(value)}toggle(value,force){const next=force===undefined?!this.values.has(value):Boolean(force);next?this.values.add(value):this.values.delete(value);this.sync();return next}}
class FakeElement extends EventTarget{constructor(tag,doc){super();this.tagName=tag.toUpperCase();this.ownerDocument=doc;this.children=[];this.parentNode=null;this.attributes=new Map();this.dataset={};this.style={};this.classList=new FakeClassList(this);this._className="";this._textContent="";this.hidden=false;this.disabled=false;this.tabIndex=-1;this.type=""}set className(value){this._className=String(value);this.classList.reset(value)}get className(){return this._className}set textContent(value){this._textContent=String(value);this.children=[]}get textContent(){return this._textContent+this.children.map(child=>child.textContent||"").join("")}append(...nodes){for(const node of nodes){if(node?.parentNode){const old=node.parentNode.children.indexOf(node);if(old>=0)node.parentNode.children.splice(old,1)}if(node&&typeof node==="object")node.parentNode=this;this.children.push(node)}}replaceChildren(...nodes){this.children=[];this._textContent="";this.append(...nodes)}setAttribute(key,value){this.attributes.set(key,String(value))}getAttribute(key){return this.attributes.get(key)??null}focus(){this.ownerDocument.activeElement=this;this.dispatchEvent(new Event("focus"))}blur(){if(this.ownerDocument.activeElement===this)this.ownerDocument.activeElement=null}getBoundingClientRect(){return{left:0,top:0,width:393,height:393,right:393,bottom:393}}}
function documentFor(dpr=3){const view=new EventTarget();view.devicePixelRatio=dpr;const doc={activeElement:null,defaultView:view};doc.createElement=tag=>new FakeElement(tag,doc);return doc}function event(type,properties={}){const value=new Event(type,{bubbles:true,cancelable:true});Object.entries(properties).forEach(([key,item])=>Object.defineProperty(value,key,{value:item}));return value}
function harness({reduced=true,viewport={width:393,height:852,dpr:3},owner=documentFor(viewport.dpr)}={}){const controller=new AbortController(),host=owner.createElement("div"),qa={},jobs=[],listeners=new Set(),finishes=[];let deadline=null,frame=null,time=0,disposed=false;const dispose=()=>{if(disposed)return;controller.abort();disposed=true;jobs.forEach(job=>job.active=false);if(deadline)deadline.active=false;frame=null;listeners.forEach(item=>item.target.removeEventListener(item.type,item.fn,item.options));listeners.clear()};const context={host,signal:controller.signal,reducedMotion:reduced,viewport,qa,finish(correct,result){if(disposed||finishes.length)return false;finishes.push({correct,result});return true},later(fn,ms){const job={fn,at:time+ms,active:true};jobs.push(job);return job},setDeadline(ms,fn){deadline={fn,at:time+ms,active:true};return deadline},frame(fn){frame=fn;return()=>{frame=null}},listen(target,type,fn,options){target.addEventListener(type,fn,options);const item={target,type,fn,options};listeners.add(item);return()=>{target.removeEventListener(type,fn,options);listeners.delete(item)}}};const runNext=()=>{const job=jobs.filter(item=>item.active).sort((a,b)=>a.at-b.at)[0];if(!job)return false;time=job.at;job.active=false;job.fn();return true},runAll=()=>{let guard=0;while(runNext()&&guard++<100);assert.ok(guard<100)},timeout=()=>{if(deadline?.active&&!disposed){deadline.active=false;deadline.fn()}};return{context,host,owner,qa,finishes,dispose,runNext,runAll,timeout,get pending(){return jobs.filter(item=>item.active).length},get frame(){return Boolean(frame)},get listeners(){return listeners.size}}}

test("metadata and exact published seven-key task are frozen",()=>{assert.deepEqual(game.metadata,{id:ID,introducedIn:"1.0",tier:1,flavor:"satisfying",step:1,family:"attention-odd",category:"attention"});assert.deepEqual(task(0,7),{kind:"oddGrid",prompt:PROMPT,help:HELP,normal:"シ",odd:"ツ",oddIndex:7,duration:7000});assert.deepEqual(Object.keys(task()),["kind","prompt","help","normal","odd","oddIndex","duration"])});
test("legacy factory is removed while renderOddGrid semantics remain pinned",()=>{assert.doesNotMatch(legacySource,/\{id:"attention-odd-v1",version:"1\.0",category:"attention",make:/);assert.match(legacySource,/function renderOddGrid\(task\)/);assert.match(legacySource,/aria-label",`\$\{i\+1\}番目 \$\{b\.textContent\}`/);assert.match(legacySource,/"見つかってしまいました。":"そっくりさんでした。"/);assert.match(legacySource,/違うものは群衆に紛れました。/)});
test("all four pairs and all 25 indices preserve pick-then-randomInt exact trace",()=>{for(let pair=0;pair<4;pair++)for(let index=0;index<25;index++){const trace=[],value=game.generate({pick(rows){trace.push(["pick",rows.map(row=>[...row])]);return rows[pair]},randomInt(min,max){trace.push(["randomInt",min,max]);return index}});assert.deepEqual(trace,[["pick",PAIRS],["randomInt",0,24]]);assert.deepEqual([value.normal,value.odd],PAIRS[pair]);assert.equal(value.oddIndex,index);assert.deepEqual(game.validate(value),[]);assert.deepEqual(structuredClone(value),value)}});
test("generation rejects missing, malformed, and out-of-range helper results",()=>{assert.throws(()=>game.generate(),/pick is required/);assert.throws(()=>game.generate({pick:items=>items[0]}),/randomInt is required/);assert.throws(()=>game.generate({pick:()=>["ツ","シ"],randomInt:()=>0}),/published pair/);for(const value of[-1,25,2.5,NaN])assert.throws(()=>game.generate({pick:items=>items[0],randomInt:()=>value}),/0 to 24/)});
test("validator rejects extras, copy, reversed pairs, alternate glyphs, index and duration retunes",()=>{const base=task(2,9),variants=[null,[],{...base,extra:1},{...base,kind:"grid"},{...base,prompt:"x"},{...base,help:"x"},{...base,normal:"B",odd:"8"},{...base,normal:"８"},{...base,oddIndex:-1},{...base,oddIndex:25},{...base,oddIndex:2.5},{...base,duration:7001}];variants.forEach((value,index)=>assert.ok(game.validate(value).length,`variant ${index}`))});
test("every pair renders exactly 25 row-major glyphs, one odd, one shared cell class, and exact aria",()=>{for(let pair=0;pair<4;pair++)for(const oddIndex of[0,12,24]){const h=harness();game.render(task(pair,oddIndex),h.context);const state=h.qa[ID].inspect();assert.equal(state.cellCount,25);assert.equal(state.oddCount,1);assert.equal(state.glyphs[oddIndex],PAIRS[pair][1]);state.glyphs.forEach((glyph,index)=>assert.equal(glyph,index===oddIndex?PAIRS[pair][1]:PAIRS[pair][0]));assert.deepEqual(state.arias,state.glyphs.map((glyph,index)=>`${index+1}番目 ${glyph}`));assert.equal(new Set(state.classes).size,1);assert.equal(state.classes[0],"aov-cell");assert.deepEqual(h.qa[ID].elements.board.children,[h.qa[ID].elements.grid,h.qa[ID].elements.banner]);assert.equal(h.qa[ID].elements.banner.dataset.visible,"false");h.dispose()}});
test("plain JSON resume chooses the stored odd and commits exact success once",()=>{const saved=JSON.parse(JSON.stringify(task(3,17))),h=harness();game.render(saved,h.context);assert.equal(h.qa[ID].choose(saved.oddIndex),true);h.runAll();assert.deepEqual(h.finishes,[{correct:true,result:{detail:"見つかってしまいました。"}}]);assert.equal(h.qa[ID].inspect().result,"success");h.qa[ID].choose(saved.oddIndex);h.timeout();assert.equal(h.finishes.length,1)});
test("choosing a normal cell commits exact wrong detail and never reveals the odd cell",()=>{const h=harness();game.render(task(1,6),h.context);assert.equal(h.qa[ID].choose(7),true);h.runAll();assert.deepEqual(h.finishes,[{correct:false,result:{detail:"そっくりさんでした。"}}]);assert.equal(h.qa[ID].inspect().selectedIndex,7);assert.equal(h.qa[ID].inspect().oddIndex,6)});
test("exact 7000ms deadline retains crowd escape detail and cannot double finish",()=>{const h=harness();game.render(task(2,24),h.context);h.timeout();assert.deepEqual(h.finishes,[{correct:false,result:{detail:"違うものは群衆に紛れました。"}}]);assert.equal(h.qa[ID].inspect().result,"timeout");h.timeout();h.qa[ID].choose(24);assert.equal(h.finishes.length,1)});
test("real touch selects its cell and busy lock rejects repeat target input",()=>{const h=harness(),value=task(0,12);game.render(value,h.context);const api=h.qa[ID],cells=api.elements.cells;cells[12].dispatchEvent(event("pointerdown",{pointerId:5,pointerType:"touch"}));assert.equal(api.inspect().selectedIndex,12);assert.equal(api.inspect().busy,true);cells[0].dispatchEvent(event("pointerdown",{pointerId:6,pointerType:"touch"}));assert.equal(api.inspect().selectedIndex,12);h.runAll();assert.equal(h.finishes[0].correct,true)});
test("keyboard navigates exact 5x5 geometry, clamps, visibly focuses, and activates",()=>{const h=harness(),value=task(3,12);game.render(value,h.context);const api=h.qa[ID],stage=api.elements.stage,cells=api.elements.cells;assert.equal(h.owner.activeElement,stage);for(let i=0;i<2;i++)stage.dispatchEvent(event("keydown",{key:"ArrowRight"}));for(let i=0;i<2;i++)stage.dispatchEvent(event("keydown",{key:"ArrowDown"}));assert.equal(api.inspect().focusIndex,12);assert.equal(h.owner.activeElement,cells[12]);stage.dispatchEvent(event("keydown",{key:"Enter"}));h.runAll();assert.equal(h.finishes[0].correct,true);const edge=harness();game.render(task(0,24),edge.context);const edgeApi=edge.qa[ID],edgeStage=edgeApi.elements.stage;for(let i=0;i<9;i++)edgeStage.dispatchEvent(event("keydown",{key:"ArrowLeft"}));for(let i=0;i<9;i++)edgeStage.dispatchEvent(event("keydown",{key:"ArrowUp"}));assert.equal(edgeApi.inspect().focusIndex,0);edge.dispose()});
test("normal owns one settle while reduced exposes two nonzero stages and no RAF",()=>{const normal=harness({reduced:false});game.render(task(0,3),normal.context);normal.qa[ID].choose(3);assert.equal(normal.pending,1);assert.equal(normal.frame,false);normal.runNext();assert.equal(normal.qa[ID].inspect().stages,1);const reduced=harness();game.render(task(0,3),reduced.context);reduced.qa[ID].choose(3);assert.equal(reduced.pending,2);assert.equal(reduced.frame,false);reduced.runNext();assert.equal(reduced.qa[ID].inspect().phase,"CHECK");reduced.runNext();assert.equal(reduced.qa[ID].inspect().stages,2);assert.equal(reduced.qa[ID].inspect().result,"success")});
test("five parity scenes have no initial clue, use a known normal focus/wrong cell, and cap DPR3",()=>{const h=harness({viewport:{width:402,height:874,dpr:4}});game.render(task(2,12),h.context);const api=h.qa[ID];assert.equal(api.inspect().dpr,3);api.showScene("initial");assert.equal(api.inspect().selectedIndex,null);assert.equal(new Set(api.inspect().classes).size,1);api.showScene("focus");const focus=api.inspect().focusIndex;assert.notEqual(focus,12);assert.equal(api.inspect().glyphs[focus],"8");assert.equal(api.inspect().selectedIndex,null);api.showScene("wrong");assert.equal(api.inspect().selectedIndex,focus);for(const scene of["initial","focus","wrong","success","timeout"])assert.equal(api.showScene(scene),true);h.dispose()});
test("ownerDocument rendering and disposal clear feedback, QA, deadline, listeners, and frames",async()=>{const staged=harness();game.render(task(0,4),staged.context);staged.qa[ID].choose(4);assert.equal(staged.pending,2);staged.dispose();staged.runAll();staged.timeout();assert.equal(staged.qa[ID],undefined);assert.equal(staged.pending,0);assert.equal(staged.listeners,0);assert.deepEqual(staged.finishes,[]);const previousRaf=globalThis.requestAnimationFrame,previousCancel=globalThis.cancelAnimationFrame;let id=0;const frames=new Map();globalThis.requestAnimationFrame=fn=>{frames.set(++id,fn);return id};globalThis.cancelAnimationFrame=key=>frames.delete(key);try{const owner=documentFor(),host=owner.createElement("div"),qa={},finishes=[],runtime=createGameRuntime({host,qa,reducedMotion:true,viewport:{width:393,height:852,dpr:3},onFinish:(correct,result)=>finishes.push({correct,result})});game.render(task(0,4),runtime.context);qa[ID].choose(4);runtime.dispose();await delay(500);assert.equal(qa[ID],undefined);assert.deepEqual(finishes,[]);assert.equal(frames.size,0);assert.deepEqual(runtime.inspect(),{disposed:true,finished:false,finishCalls:0,commits:0,timeouts:0,frames:0,listeners:0,aborted:true})}finally{if(previousRaf===undefined)delete globalThis.requestAnimationFrame;else globalThis.requestAnimationFrame=previousRaf;if(previousCancel===undefined)delete globalThis.cancelAnimationFrame;else globalThis.cancelAnimationFrame=previousCancel}});
test("source preserves literal glyph typography and uses only tracked offline ownerDocument paths",()=>{assert.match(source,/context\.host\?\.ownerDocument/);assert.match(source,/writing-mode:horizontal-tb/);assert.match(source,/font-style:normal/);assert.match(source,/\.aov-banner\{position:relative/);assert.doesNotMatch(source,/\.aov-banner\{position:absolute/);assert.doesNotMatch(source,/\b(?:rotate|skew)\s*\(/);assert.doesNotMatch(source,/\bdocument\.createElement\s*\(/);assert.doesNotMatch(source,/\b(?:setTimeout|setInterval|requestAnimationFrame|cancelAnimationFrame|addEventListener)\s*\(/);assert.doesNotMatch(source,/\b(?:fetch|XMLHttpRequest|WebSocket|EventSource|sendBeacon|AudioContext|HTMLAudioElement|localStorage|sessionStorage)\b/);assert.doesNotMatch(source,/https?:\/\//);assert.doesNotMatch(source,/context\.frame/);assert.match(source,/context\.later/);assert.match(source,/context\.setDeadline/);assert.match(source,/context\.listen/)});

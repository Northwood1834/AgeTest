import game from "../src/games/inhibition-parity-v1.js";
import {createGameRuntime} from "../src/game-kernel.js";
const NUMBERS=[12,13,38,39],TASKS=Object.freeze(NUMBERS.map(number=>({kind:"choice",prompt:`${number} は偶数。正しければ？`,help:"文章を最後まで読んで判断。",options:["押す","押さない"],answer:number%2===0?"押す":"押さない",duration:5000}))),params=new URLSearchParams(location.search),sample=Math.max(0,Math.min(3,Number(params.get("sample"))||0)),TASK=TASKS[sample],width=Number(params.get("width"))||393,motion=params.get("motion")==="reduced"?"reduced":"normal",scene=params.get("scene")||"initial",reduced=motion==="reduced",live=params.get("live")==="1",host=document.querySelector("#challenge"),qa={},finishes=[],errors=[];document.body.style.width=`${width}px`;document.querySelector("#question-title").textContent=TASK.prompt;addEventListener("error",event=>errors.push(event.message||"page error"));addEventListener("unhandledrejection",event=>errors.push(String(event.reason)));
const runtime=createGameRuntime({host,qa,reducedMotion:reduced,viewport:{width,height:innerHeight,dpr:devicePixelRatio},onFinish:(correct,result)=>finishes.push({correct,result})}),startedAt=performance.now();game.render(structuredClone(TASK),runtime.context);const api=qa[game.metadata.id];if(!live&&!api.showScene(scene))throw new Error(`unknown scene ${scene}`);
const terminal=state=>state.result==="success"?{correct:true,result:{answerLabel:TASK.answer}}:state.result==="wrong"?{correct:false,result:{answerLabel:TASK.answer}}:state.result==="timeout"?{correct:false,result:{detail:"時間切れです。脳は定時退社しました。",answerLabel:TASK.answer}}:null,evidence=()=>{const resources=performance.getEntriesByType("resource"),external=resources.filter(entry=>new URL(entry.name).origin!==location.origin),state=api.inspect(),number=document.querySelector(".ip-number"),sceneNode=document.querySelector(".ip-scene"),numberStyle=getComputedStyle(number),sceneStyle=getComputedStyle(sceneNode);return{variant:"module",sample,scene,live,motion,width,dpr:devicePixelRatio,task:structuredClone(TASK),constants:{min:12,max:39,samples:NUMBERS,duration:5000},state,computed:{number:state.number,prompt:state.prompt,answer:state.answer,options:state.buttons.map(button=>button.text),choicesEnabled:state.choicesEnabled,result:state.result,selected:state.selected,payload:terminal(state),feedbackStages:state.feedbackStages,focused:state.buttons.findIndex(button=>button.focused),numberClass:number.className,numberChildCount:number.childElementCount,numberStyle:{color:numberStyle.color,fontSize:numberStyle.fontSize,fontWeight:numberStyle.fontWeight,letterSpacing:numberStyle.letterSpacing,opacity:numberStyle.opacity},sceneStyle:{backgroundColor:sceneStyle.backgroundColor,borderColor:sceneStyle.borderColor,borderRadius:sceneStyle.borderRadius}},finishes:structuredClone(finishes),layout:{scrollWidth:document.body.scrollWidth,innerWidth,canvas:state.canvas},resources:resources.map(entry=>entry.name),external:external.map(entry=>entry.name),errors:[...errors]}};
window.__PARITY_MODULE_FIXTURE__={game,numbers:NUMBERS,tasks:structuredClone(TASKS),task:structuredClone(TASK),sample,qa,api,runtime,startedAt,evidence};document.documentElement.dataset.ready="true";

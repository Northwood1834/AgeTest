import game from "../src/games/prediction-ricochet-knockback-v1.js";
import {createGameRuntime} from "../src/game-kernel.js";

const params=new URLSearchParams(location.search),scene=params.get("scene")||"initial",reduced=params.get("motion")==="reduced",width=Number(params.get("width"))||393,layout=Math.max(0,Math.min(2,Number(params.get("layout"))||0)),deadlineOverride=Math.max(0,Number(params.get("deadline"))||0);document.body.style.width=`${width}px`;
const task=game.generate({randomInt:()=>layout}),host=document.querySelector("#challenge"),label=document.querySelector("#scene-label"),resultNode=document.querySelector("#fixture-result"),qa={},errors=[],started=performance.now();let outcome=null;
addEventListener("error",event=>errors.push(String(event.error?.stack||event.message)));addEventListener("unhandledrejection",event=>errors.push(String(event.reason?.stack||event.reason)));
const runtime=createGameRuntime({host,qa,reducedMotion:reduced,viewport:{width,height:innerHeight,dpr:devicePixelRatio},onFinish:(correct,detail)=>{outcome={correct,detail};resultNode.textContent=`${correct?"成功":"未完了"} · ${detail.outcome}`}}),renderContext=deadlineOverride?{...runtime.context,setDeadline:(_ms,fn)=>runtime.context.setDeadline(deadlineOverride,fn)}:runtime.context;game.render(structuredClone(task),renderContext);const api=qa[game.metadata.id];
async function runScene(name){if(name==="timeout")api.timeout();else if(name!=="initial"&&!api.showScene(name))throw new Error(`unknown scene: ${name}`);await new Promise(resolve=>requestAnimationFrame(()=>requestAnimationFrame(resolve)));document.documentElement.dataset.ready="true";document.body.dataset.scene=name;document.body.dataset.motion=reduced?"reduced":"normal";label.textContent=`${task.layout} · ${name}`}
await runScene(scene);
const report=()=>{const state=api?.inspect()||null,resources=performance.getEntriesByType("resource"),external=resources.filter(entry=>new URL(entry.name).origin!==location.origin),targets=[...document.querySelectorAll(".prk-control,.prk-board")].map(node=>{const rect=node.getBoundingClientRect();return{label:node.textContent||node.getAttribute("aria-label"),width:rect.width,height:rect.height}});return{status:external.length||document.body.scrollWidth>width||errors.length?"fail":"pass",scene,reduced,width,layout,task:structuredClone(task),game:state,runtime:runtime.inspect(),outcome,elapsedMs:performance.now()-started,viewport:{width:innerWidth,height:innerHeight,dpr:devicePixelRatio,bodyScrollWidth:document.body.scrollWidth,bodyScrollHeight:document.body.scrollHeight},targets,resources:resources.map(entry=>entry.name),external:external.map(entry=>entry.name),errors:[...errors]}};
window.__RICOCHET_FIXTURE__={game,task,qa,runtime,scene,reduced,report,inspect:()=>api?.inspect()||null,errors:()=>[...errors]};

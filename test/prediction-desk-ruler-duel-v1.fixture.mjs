import game from "../src/games/prediction-desk-ruler-duel-v1.js";import{createGameRuntime}from"../src/game-kernel.js";
const params=new URLSearchParams(location.search),width=Number(params.get("width"))||393,reduced=params.get("motion")==="reduced",auto=params.get("auto")==="1";document.body.style.width=`${width}px`;const task=game.generate({randomInt:()=>0}),host=document.querySelector("#challenge"),result=document.querySelector("#result"),qa={},frameDeltas=[];let previous=performance.now(),monitoring=true,runtime,controls=null,initial=null;const monitor=now=>{if(!monitoring)return;frameDeltas.push(now-previous);previous=now;if(frameDeltas.length<900)requestAnimationFrame(monitor)};requestAnimationFrame(monitor);
const finish=(correct,detail)=>{monitoring=false;result.hidden=false;result.textContent=`${correct?"成功":"失敗"} — ${detail.detail}`;if(!auto)return;setTimeout(()=>{const inspection=runtime.inspect(),failures=[];if(!correct)failures.push("solve failed");if(!controls?.touch||!controls?.keyboard)failures.push("controls failed");if(!(inspection.disposed&&inspection.aborted&&!inspection.timeouts&&!inspection.frames&&!inspection.listeners))failures.push("cleanup failed");if(document.body.scrollWidth>width)failures.push("overflow");const report={status:failures.length?"fail":"pass",failures,correct,detail,controls,viewport:{width,dpr:devicePixelRatio,pointerEvents:"PointerEvent"in window,bodyScrollWidth:document.body.scrollWidth},canvas:initial.canvas,runtime:inspection,resources:performance.getEntriesByType("resource").map(x=>new URL(x.name).pathname)};const pre=document.createElement("pre");pre.dataset.status=report.status;pre.textContent=JSON.stringify(report,null,2);document.body.replaceChildren(pre)},0)};
runtime=createGameRuntime({host,reducedMotion:reduced,viewport:{width,height:innerHeight,dpr:devicePixelRatio},qa,onFinish:finish});game.render(task,runtime.context);const api=qa[game.metadata.id];initial=api.inspect();
if(auto){const canvas=host.querySelector(".pdr-canvas"),rect=canvas.getBoundingClientRect(),sx=rect.left+rect.width*.55,sy=rect.top+rect.height*.5;canvas.dispatchEvent(new PointerEvent("pointerdown",{bubbles:true,cancelable:true,pointerId:9,pointerType:"touch",clientX:sx,clientY:sy}));canvas.dispatchEvent(new PointerEvent("pointermove",{bubbles:true,cancelable:true,pointerId:9,pointerType:"touch",clientX:rect.left+rect.width*.33,clientY:rect.top+rect.height*.59}));canvas.dispatchEvent(new PointerEvent("pointerup",{bubbles:true,cancelable:true,pointerId:9,pointerType:"touch",clientX:rect.left+rect.width*.33,clientY:rect.top+rect.height*.59}));const touch=api.inspect().shots===1;controls={touch,keyboard:false};const finishByKeyboard=()=>{if(api.inspect().busy){runtime.context.later(finishByKeyboard,40);return}canvas.dispatchEvent(new KeyboardEvent("keydown",{bubbles:true,cancelable:true,key:"ArrowLeft"}));canvas.dispatchEvent(new KeyboardEvent("keydown",{bubbles:true,cancelable:true,key:"Enter"}));controls.keyboard=document.activeElement===canvas};runtime.context.later(finishByKeyboard,reduced?280:40)}
const scene=params.get("scene");if(scene)api.showScene(scene);
if(params.get("measureLog")==="1")setTimeout(()=>{monitoring=false;const stable=frameDeltas.slice(5),inspection=api.inspect(),query=new URLSearchParams({frames:String(stable.length),averageFrameMs:(stable.reduce((a,b)=>a+b,0)/Math.max(1,stable.length)).toFixed(3),maxFrameMs:Math.max(0,...stable).toFixed(3),gameFrames:String(inspection.frames),pixelWidth:String(inspection.canvas.pixelWidth),pixelHeight:String(inspection.canvas.pixelHeight),dpr:String(inspection.canvas.dpr)});location.replace(`prediction-desk-ruler-duel-v1.performance-result.html?${query}`)},1500);
window.__RULER_FIXTURE__={task,qa,runtime,evidence:()=>({game:qa[game.metadata.id]?.inspect(),runtime:runtime.inspect()})};

#!/usr/bin/env node
import {execFile} from "node:child_process";
import {writeFile} from "node:fs/promises";
import {promisify} from "node:util";

const exec=promisify(execFile),tool="tools/qa-browser-lanes.mjs",lane="screw",root="http://127.0.0.1:8863/test/social-thread-vibe-v1.fixture.html";
const call=async args=>(await exec(process.execPath,[tool,...args],{cwd:new URL("..",import.meta.url).pathname,maxBuffer:4*1024*1024})).stdout.trim();
const parse=value=>JSON.parse(value),navigate=async query=>{const url=`${root}?${new URLSearchParams({...query,run:String(Date.now())})}`;await call(["navigate",lane,url,String(query.width),String(query.height),"3"]);const ready=parse(await call(["eval",lane,"(async()=>{for(let i=0;i<120&&!document.documentElement.dataset.ready;i++)await new Promise(r=>setTimeout(r,50));return document.documentElement.dataset.ready||'timeout'})()"]));if(ready!=="true")throw new Error("fixture not ready")};
const status=parse(await call(["status",lane]));if(!status[0]?.ready||status[0].cdp!==9343||status[0].http!==8863)throw new Error("screw lane unavailable");

await navigate({scene:"initial",motion:"normal",width:393,height:852,family:1});
const interaction=parse(await call(["eval",lane,`(async()=>{const fixture=window.__STV_FIXTURE__,api=fixture.qa[fixture.game.metadata.id],stage=document.querySelector('.stv-stage'),buttons=[...document.querySelectorAll('.stv-option')],box=buttons[1].getBoundingClientRect(),touch=[box.width,box.height];stage.dispatchEvent(new KeyboardEvent('keydown',{key:'ArrowDown',bubbles:true,cancelable:true}));const firstFocus=document.activeElement===buttons[0];stage.dispatchEvent(new KeyboardEvent('keydown',{key:'ArrowDown',bubbles:true,cancelable:true}));const secondFocus=document.activeElement===buttons[1];buttons[1].dispatchEvent(new PointerEvent('pointerdown',{pointerType:'touch',bubbles:true,cancelable:true}));buttons[1].dispatchEvent(new MouseEvent('click',{detail:1,bubbles:true,cancelable:true}));const immediate=api.inspect();await new Promise(r=>setTimeout(r,720));const settled=api.inspect();stage.dispatchEvent(new KeyboardEvent('keydown',{key:'2',bubbles:true,cancelable:true}));const keyboard=api.inspect();return{firstFocus,secondFocus,immediate:{path:immediate.path,posts:immediate.posts.length,busy:immediate.busy},settled:{turn:settled.turn,path:settled.path,posts:settled.posts.length,busy:settled.busy},keyboard:{path:keyboard.path,posts:keyboard.posts.length,busy:keyboard.busy},dpr:keyboard.dpr,overflow:document.body.scrollWidth-innerWidth,touch,runtime:fixture.runtime.inspect()}})()`]));
if(!interaction.firstFocus||!interaction.secondFocus||interaction.immediate.path.length!==1||interaction.immediate.posts!==5||interaction.settled.turn!==1||interaction.settled.posts!==8||interaction.keyboard.path.length!==2||interaction.dpr!==3||interaction.overflow>0||interaction.touch[1]<48)throw new Error("browser input evidence failed");

await navigate({scene:"initial",motion:"normal",width:402,height:874,family:3});
const disposal=parse(await call(["eval",lane,`(async()=>{const fixture=window.__STV_FIXTURE__,id=fixture.game.metadata.id,api=fixture.qa[id];api.select(fixture.task.answer[0]);const before=api.inspect();fixture.runtime.dispose();await new Promise(r=>setTimeout(r,850));const after=api.inspect();return{before:{posts:before.posts.length,path:before.path},after:{posts:after.posts.length,path:after.path,disposed:after.disposed,result:after.result,finishCalls:after.finishCalls},qaRemoved:fixture.qa[id]===undefined,runtime:fixture.runtime.inspect(),outcome:fixture.report().outcome}})()`]));
if(disposal.before.posts!==disposal.after.posts||!disposal.after.disposed||disposal.after.finishCalls!==0||!disposal.qaRemoved||!disposal.runtime.disposed||disposal.runtime.listeners!==0||disposal.runtime.timeouts!==0||disposal.outcome!==null)throw new Error("browser disposal evidence failed");

await navigate({scene:"progress",motion:"reduced",width:402,height:874,family:2});
const reduced=parse(await call(["eval",lane,`(()=>{const fixture=window.__STV_FIXTURE__,r=fixture.report(),stage=document.querySelector('.stv-stage'),arrivals=[...document.querySelectorAll('.stv-new')];return{elapsedMs:r.elapsedMs,scene:r.scene,turn:r.game.turn,posts:r.game.posts.length,dpr:r.game.dpr,reduced:stage.dataset.reduced,animationsDisabled:arrivals.every(node=>getComputedStyle(node).animationName==='none'),runtime:r.runtime,overflow:r.viewport.bodyScrollWidth-r.width,resourcesAdded:r.resourcesAdded,external:r.external,nodeCount:document.querySelectorAll('*').length,navigationMs:performance.getEntriesByType('navigation')[0]?.duration??null}})()`]));
if(reduced.turn!==1||reduced.posts!==8||reduced.dpr!==3||reduced.reduced!=="true"||!reduced.animationsDisabled||reduced.runtime.frames!==0||reduced.overflow>0||reduced.resourcesAdded||reduced.external.length||reduced.nodeCount>180||reduced.elapsedMs>1800)throw new Error("reduced/performance evidence failed");

const evidence={game:"social-thread-vibe-v1",lane:{name:lane,cdp:9343,http:8863,browser:status[0].browser},checks:{keyboardFocus:interaction.firstFocus&&interaction.secondFocus,touchSingleCommit:interaction.immediate.path.length===1,keyboardSelection:interaction.keyboard.path.length===2,dpr3:interaction.dpr===3&&reduced.dpr===3,noOverflow:interaction.overflow<=0&&reduced.overflow<=0,trackedDispose:disposal.qaRemoved&&disposal.runtime.listeners===0&&disposal.runtime.timeouts===0,reducedMotion:reduced.animationsDisabled&&reduced.runtime.frames===0,performance:reduced.elapsedMs<=1800&&reduced.nodeCount<=180,noExternalResources:!reduced.resourcesAdded&&!reduced.external.length},interaction,disposal,reduced};
await writeFile("test/social-thread-vibe-v1.browser-evidence.json",`${JSON.stringify(evidence,null,2)}\n`);console.log(JSON.stringify(evidence.checks));
